-- MySQL Script generated by MySQL Workbench
-- Wed Jul  1 18:18:44 2026
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema gluttex
-- -----------------------------------------------------
DROP SCHEMA IF EXISTS `gluttex` ;

-- -----------------------------------------------------
-- Schema gluttex
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `gluttex` ;
-- -----------------------------------------------------
-- Schema gluttex
-- -----------------------------------------------------
DROP SCHEMA IF EXISTS `gluttex` ;

-- -----------------------------------------------------
-- Schema gluttex
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `gluttex` ;
USE `gluttex` ;

-- -----------------------------------------------------
-- Table `gluttex`.`plan`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`plan` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`plan` (
  `id_plan` INT NOT NULL AUTO_INCREMENT,
  `plan_name` VARCHAR(45) NULL DEFAULT NULL,
  `plan_price` DECIMAL(10,2) NULL DEFAULT NULL,
  `billing_cycle` ENUM('monthly', 'yearly') NULL DEFAULT 'monthly',
  `plan_type` ENUM('individual', 'organization') NULL DEFAULT 'individual',
  `plan_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `plan_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id_plan`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`naming_contribution`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`naming_contribution` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`naming_contribution` (
  `id_naming_contribution` INT NOT NULL AUTO_INCREMENT,
  `naming_contribution_ar` VARCHAR(255) NULL,
  `naming_contribution_fr` VARCHAR(255) NULL,
  `naming_contribution_en` VARCHAR(255) NULL,
  `naming_contribution_status` ENUM('PENDING', 'ACCEPTED', 'APP_TRANSLATED', 'REJECTED') NULL DEFAULT 'PENDING',
  `naming_contribution_icon_url` VARCHAR(255) NULL,
  `naming_contribution_by` INT NULL,
  `naming_contribution_app_version` VARCHAR(45) NULL,
  `naming_contribution_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `naming_contribution_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `naming_contribution_type` ENUM('product', 'provider', 'ingredient', 'recipe', 'service', 'symptom', 'role') NULL,
  PRIMARY KEY (`id_naming_contribution`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`provided_service_category`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`provided_service_category` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`provided_service_category` (
  `provided_service_category_id` INT NOT NULL AUTO_INCREMENT,
  `provided_service_category_name` VARCHAR(255) NULL,
  `provided_service_category_naming_ref` INT NULL,
  `provided_service_category_icon_url` VARCHAR(500) NULL,
  `provided_service_category_avg_duration` DECIMAL(10,2) NULL COMMENT 'Average duration in minutes',
  `provided_service_category_description` TEXT NULL DEFAULT NULL,
  `provided_service_category_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `provided_service_category_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `provided_service_category_deleted_at` TIMESTAMP NULL DEFAULT NULL,
  PRIMARY KEY (`provided_service_category_id`),
  INDEX `idx_provided_service_category_name` (`provided_service_category_naming_ref` ASC) VISIBLE,
  INDEX `idx_provided_service_category_created_at` (`provided_service_category_created_at` ASC) VISIBLE,
  INDEX `idx_provided_service_category_active` (`provided_service_category_deleted_at` ASC) VISIBLE,
  CONSTRAINT `fk_provided_service_category_1`
    FOREIGN KEY (`provided_service_category_naming_ref`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`staff_role`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`staff_role` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`staff_role` (
  `id_staff_role` INT NOT NULL AUTO_INCREMENT,
  `staff_role_service_category_ref` INT NULL DEFAULT NULL,
  `staff_role_naming_ref` INT NULL,
  `staff_role_icon_url` VARCHAR(255) NULL,
  `staff_role_name` VARCHAR(255) NULL,
  PRIMARY KEY (`id_staff_role`),
  INDEX `fk_staff_role_1_idx` (`staff_role_service_category_ref` ASC) VISIBLE,
  INDEX `fk_staff_role_2_idx` (`staff_role_naming_ref` ASC) VISIBLE,
  CONSTRAINT `fk_staff_role_1`
    FOREIGN KEY (`staff_role_service_category_ref`)
    REFERENCES `gluttex`.`provided_service_category` (`provided_service_category_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_staff_role_2`
    FOREIGN KEY (`staff_role_naming_ref`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`person_details`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`person_details` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`person_details` (
  `id_person_details` INT NOT NULL AUTO_INCREMENT,
  `person_first_name` VARCHAR(255) NULL DEFAULT NULL,
  `person_last_name` VARCHAR(255) NULL DEFAULT NULL,
  `person_birth_date` DATE NULL DEFAULT NULL,
  `person_gender` ENUM('Male', 'Female') NULL DEFAULT NULL,
  `person_country_code` VARCHAR(45) NULL DEFAULT NULL,
  `person_phone` VARCHAR(45) NULL,
  `person_languages` VARCHAR(100) NULL,
  `person_id_number` VARCHAR(100) NULL,
  `person_id_type` ENUM('Passport', 'National_ID', 'driving_license') NULL,
  `person_id_expiry` DATE NULL,
  `person_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `person_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `person_job_title` VARCHAR(255) NULL,
  `person_job_ref` INT NULL,
  `verified_person_details` TINYINT NULL,
  PRIMARY KEY (`id_person_details`),
  INDEX `fk_person_details_1_idx` (`person_job_ref` ASC) VISIBLE,
  CONSTRAINT `fk_person_details_1`
    FOREIGN KEY (`person_job_ref`)
    REFERENCES `gluttex`.`staff_role` (`id_staff_role`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`address`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`address` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`address` (
  `id_address` INT NOT NULL AUTO_INCREMENT,
  `address_street` VARCHAR(45) NULL DEFAULT NULL,
  `address_city` VARCHAR(45) NULL DEFAULT NULL,
  `address_postal_code` VARCHAR(45) NULL DEFAULT NULL,
  `address_country` VARCHAR(45) NULL DEFAULT NULL,
  PRIMARY KEY (`id_address`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`location`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`location` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`location` (
  `id_location` INT NOT NULL AUTO_INCREMENT,
  `location_position` POINT NOT NULL,
  `location_name` VARCHAR(45) NULL DEFAULT NULL,
  `location_address_id` INT NULL DEFAULT NULL,
  `location_postal_code` INT NULL,
  PRIMARY KEY (`id_location`),
  INDEX `fk_location_1_idx` (`location_address_id` ASC) VISIBLE,
  SPATIAL INDEX `spatial` (`location_position`) VISIBLE,
  CONSTRAINT `fk_location_1`
    FOREIGN KEY (`location_address_id`)
    REFERENCES `gluttex`.`address` (`id_address`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`care_giver`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`care_giver` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`care_giver` (
  `id_care_giver` INT NOT NULL AUTO_INCREMENT,
  `staff_role_id` INT NULL,
  `verified_care_giver` TINYINT NULL,
  `care_giver_license` VARCHAR(255) NULL,
  `care_giver_type` ENUM('DOCTOR', 'PHARMACIST', 'PHYSICIAN', 'DIETITIAN', 'PHYSICAL_THERAPIST', 'NURSE') NULL,
  PRIMARY KEY (`id_care_giver`),
  INDEX `fk_doctor_staff_role1_idx` (`staff_role_id` ASC) VISIBLE,
  CONSTRAINT `fk_care_giver_staff_role1`
    FOREIGN KEY (`staff_role_id`)
    REFERENCES `gluttex`.`staff_role` (`id_staff_role`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`person`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`person` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`person` (
  `id_person` INT NOT NULL AUTO_INCREMENT,
  `person_details_id` INT NULL DEFAULT NULL,
  `person_blood_type` ENUM('A+', 'A-', 'B+', 'B-', 'AB+', 'AB-', 'O+', 'O-') NULL DEFAULT NULL,
  `person_location_id` INT NULL DEFAULT NULL,
  `doctor_id` INT NULL,
  PRIMARY KEY (`id_person`),
  INDEX `fk_person_1_idx` (`person_details_id` ASC) VISIBLE,
  INDEX `fk_person_3_idx` (`person_location_id` ASC) VISIBLE,
  INDEX `fk_person_doctor1_idx` (`doctor_id` ASC) VISIBLE,
  CONSTRAINT `fk_person_1`
    FOREIGN KEY (`person_details_id`)
    REFERENCES `gluttex`.`person_details` (`id_person_details`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_person_3`
    FOREIGN KEY (`person_location_id`)
    REFERENCES `gluttex`.`location` (`id_location`),
  CONSTRAINT `fk_person_doctor1`
    FOREIGN KEY (`doctor_id`)
    REFERENCES `gluttex`.`care_giver` (`id_care_giver`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`wallet`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`wallet` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`wallet` (
  `id_wallet` INT NOT NULL AUTO_INCREMENT,
  `wallet_type` ENUM('user', 'provider', 'organization', 'system', 'virtual', 'business') NULL DEFAULT 'user',
  `wallet_currency` ENUM('DZD', 'USD', 'EUR') NULL DEFAULT 'DZD',
  `wallet_balance` DECIMAL(20,8) NULL,
  `wallet_status` ENUM('active', 'inactive', 'suspended', 'closed', 'pending_verification') NULL DEFAULT 'pending_verification',
  `wallet_version` INT NULL DEFAULT 0,
  PRIMARY KEY (`id_wallet`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`app_user`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`app_user` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`app_user` (
  `id_app_user` INT NOT NULL AUTO_INCREMENT,
  `app_user_name` VARCHAR(100) NULL DEFAULT NULL,
  `app_user_password` VARCHAR(256) NULL DEFAULT NULL,
  `app_user_person_id` INT NULL DEFAULT NULL,
  `app_user_type` ENUM('admin', 'provider', 'customer', 'patient', 'guest') NULL DEFAULT 'customer',
  `app_user_preferences` TEXT NULL DEFAULT NULL,
  `app_user_image_url` VARCHAR(255) NULL DEFAULT NULL,
  `app_user_last_active` TIMESTAMP NULL DEFAULT NULL,
  `app_user_last_updated` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `app_user_creation` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `app_user_subscription_ref` INT NULL DEFAULT NULL,
  `app_user_email` VARCHAR(255) NULL,
  `app_user_wallet_id` INT NULL,
  `app_user_login_option` ENUM('google', 'gluttex') NULL,
  `verified_app_user` TINYINT NULL,
  PRIMARY KEY (`id_app_user`),
  INDEX `fk_app_user_3_idx` (`app_user_person_id` ASC) VISIBLE,
  INDEX `fk_app_user_2_idx` (`app_user_subscription_ref` ASC) VISIBLE,
  INDEX `fk_app_user_4_idx` (`app_user_wallet_id` ASC) VISIBLE,
  CONSTRAINT `fk_app_user_2`
    FOREIGN KEY (`app_user_subscription_ref`)
    REFERENCES `gluttex`.`plan` (`id_plan`),
  CONSTRAINT `fk_app_user_3`
    FOREIGN KEY (`app_user_person_id`)
    REFERENCES `gluttex`.`person` (`id_person`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_app_user_4`
    FOREIGN KEY (`app_user_wallet_id`)
    REFERENCES `gluttex`.`wallet` (`id_wallet`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`product_provider_type`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`product_provider_type` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`product_provider_type` (
  `id_product_provider_type` INT NOT NULL AUTO_INCREMENT,
  `product_provider_type_naming_ref` INT NULL DEFAULT NULL,
  `product_provider_type_icon_url` VARCHAR(255) NULL,
  `product_provider_type_name` VARCHAR(255) NULL,
  PRIMARY KEY (`id_product_provider_type`),
  INDEX `fk_product_provider_type_1_idx` (`product_provider_type_naming_ref` ASC) VISIBLE,
  CONSTRAINT `fk_product_provider_type_1`
    FOREIGN KEY (`product_provider_type_naming_ref`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`provider_organisation`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`provider_organisation` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`provider_organisation` (
  `idprovider_organisation` INT NOT NULL AUTO_INCREMENT,
  `provider_organisation_naming` INT NULL DEFAULT NULL,
  `provider_organisation_wallet_id` INT NULL,
  `provider_organisation_name` VARCHAR(255) NULL,
  `provider_organisation_icon_url` VARCHAR(255) NULL,
  `provider_organisation_desc` VARCHAR(255) NULL,
  `app_user_id` INT NULL,
  `verified_organisation` TINYINT NULL,
  PRIMARY KEY (`idprovider_organisation`),
  INDEX `fk_provider_organisation_1_idx` (`provider_organisation_wallet_id` ASC) VISIBLE,
  INDEX `fk_provider_organisation_2_idx` (`provider_organisation_naming` ASC) VISIBLE,
  INDEX `fk_provider_organisation_app_user1_idx` (`app_user_id` ASC) VISIBLE,
  CONSTRAINT `fk_provider_organisation_1`
    FOREIGN KEY (`provider_organisation_wallet_id`)
    REFERENCES `gluttex`.`wallet` (`id_wallet`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_provider_organisation_2`
    FOREIGN KEY (`provider_organisation_naming`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_provider_organisation_app_user1`
    FOREIGN KEY (`app_user_id`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`provider_details`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`provider_details` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`provider_details` (
  `idprovider_details_id` INT NOT NULL AUTO_INCREMENT,
  `provider_naming_ref` INT NULL DEFAULT NULL,
  `provider_contact_info` TEXT NULL DEFAULT NULL,
  `provider_name` VARCHAR(255) NULL,
  `provider_details_icon_url` VARCHAR(255) NULL,
  PRIMARY KEY (`idprovider_details_id`),
  INDEX `fk_provider_details_1_idx` (`provider_naming_ref` ASC) VISIBLE,
  CONSTRAINT `fk_provider_details_1`
    FOREIGN KEY (`provider_naming_ref`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`product_provider`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`product_provider` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`product_provider` (
  `id_product_provider` INT NOT NULL AUTO_INCREMENT,
  `product_provider_details_id` INT NULL DEFAULT NULL,
  `product_provider_type_id` INT NULL DEFAULT NULL,
  `product_provider_location_id` INT NULL DEFAULT NULL,
  `product_provider_org_id` INT NULL DEFAULT NULL,
  `product_provider_owner` INT NULL DEFAULT NULL,
  `product_provider_wallet_id` INT NULL,
  `verified_provider` TINYINT NULL,
  PRIMARY KEY (`id_product_provider`),
  INDEX `fk_product_provider_3_idx` (`product_provider_details_id` ASC) VISIBLE,
  INDEX `fk_product_provider_4_idx` (`product_provider_location_id` ASC) VISIBLE,
  INDEX `fk_product_provider_2_idx` (`product_provider_org_id` ASC) VISIBLE,
  INDEX `fk_product_provider_5_idx` (`product_provider_owner` ASC) VISIBLE,
  INDEX `fk_product_provider_1_idx` (`product_provider_type_id` ASC) VISIBLE,
  INDEX `fk_product_provider_6_idx` (`product_provider_wallet_id` ASC) VISIBLE,
  CONSTRAINT `fk_product_provider_1`
    FOREIGN KEY (`product_provider_type_id`)
    REFERENCES `gluttex`.`product_provider_type` (`id_product_provider_type`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_product_provider_2`
    FOREIGN KEY (`product_provider_org_id`)
    REFERENCES `gluttex`.`provider_organisation` (`idprovider_organisation`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_product_provider_3`
    FOREIGN KEY (`product_provider_details_id`)
    REFERENCES `gluttex`.`provider_details` (`idprovider_details_id`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_product_provider_4`
    FOREIGN KEY (`product_provider_location_id`)
    REFERENCES `gluttex`.`location` (`id_location`),
  CONSTRAINT `fk_product_provider_5`
    FOREIGN KEY (`product_provider_owner`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`),
  CONSTRAINT `fk_product_provider_6`
    FOREIGN KEY (`product_provider_wallet_id`)
    REFERENCES `gluttex`.`wallet` (`id_wallet`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`invoice`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`invoice` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`invoice` (
  `invoice_id` INT NOT NULL AUTO_INCREMENT,
  `invoice_number` VARCHAR(100) NULL,
  `invoice_total_amount` DECIMAL(15,4) NULL,
  `invoice_status` ENUM('unpaid', 'paid', 'canceled', 'partially_paid', 'overdue', 'refunded') NULL DEFAULT 'unpaid' COMMENT 'unpaid, paid, canceled',
  `invoice_issue_date` DATE NULL,
  `invoice_due_date` DATE NULL DEFAULT NULL,
  `invoice_notes` TEXT NULL DEFAULT NULL,
  `invoice_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `invoice_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `invoice_type` ENUM('receipt', 'invoice', 'proforma') NULL DEFAULT 'receipt',
  `invoice_tax_applied` TINYINT NULL,
  PRIMARY KEY (`invoice_id`),
  INDEX `idx_invoice_status` (`invoice_status` ASC) VISIBLE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`additional_fee`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`additional_fee` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`additional_fee` (
  `additional_fee_id` INT NOT NULL AUTO_INCREMENT,
  `additional_fee_name` VARCHAR(255) NULL,
  `additional_fee_amount` DECIMAL(15,4) NULL,
  `additional_fee_description` TEXT NULL DEFAULT NULL,
  `additional_fee_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `additional_fee_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `additional_fee_document_url` VARCHAR(255) NULL,
  `additional_fee_user_id` INT NULL,
  `additional_fee_on_provider_id` INT NULL,
  `additional_fee_invoice` INT NULL,
  PRIMARY KEY (`additional_fee_id`),
  INDEX `fk_additional_fee_1_idx` (`additional_fee_user_id` ASC) VISIBLE,
  INDEX `fk_additional_fee_2_idx` (`additional_fee_on_provider_id` ASC) VISIBLE,
  INDEX `fk_additional_fee_3_idx` (`additional_fee_invoice` ASC) VISIBLE,
  CONSTRAINT `fk_additional_fee_1`
    FOREIGN KEY (`additional_fee_user_id`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_additional_fee_2`
    FOREIGN KEY (`additional_fee_on_provider_id`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_additional_fee_3`
    FOREIGN KEY (`additional_fee_invoice`)
    REFERENCES `gluttex`.`invoice` (`invoice_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`cart`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`cart` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`cart` (
  `cart_id` INT NOT NULL AUTO_INCREMENT,
  `cart_product_provider_id` INT NULL COMMENT 'Provider owning the cart',
  `cart_selling_user` INT NULL COMMENT 'Customer / patient / client id',
  `cart_status` ENUM('open', 'pending', 'completed', 'canceled', 'partial', 'checkout', 'abandoned') NULL DEFAULT 'open' COMMENT 'open, pending, completed, canceled',
  `cart_total_amount` DECIMAL(15,4) NULL DEFAULT '0.0000',
  `cart_notes` TEXT NULL DEFAULT NULL,
  `cart_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `cart_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `cart_person_ref` INT NULL,
  `cart_client_user` INT NULL,
  `cart_due_date` DATE NULL DEFAULT NULL,
  `cart_invoice` INT NULL,
  PRIMARY KEY (`cart_id`),
  INDEX `idx_cart_provider` (`cart_product_provider_id` ASC) VISIBLE,
  INDEX `idx_cart_user` (`cart_selling_user` ASC) VISIBLE,
  INDEX `idx_cart_status` (`cart_status` ASC) VISIBLE,
  INDEX `fk_cart_3_idx` (`cart_person_ref` ASC) VISIBLE,
  INDEX `fk_cart_5_idx` (`cart_invoice` ASC) VISIBLE,
  CONSTRAINT `cart_ibfk_1`
    FOREIGN KEY (`cart_product_provider_id`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE,
  CONSTRAINT `fk_cart_1`
    FOREIGN KEY (`cart_selling_user`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_cart_2`
    FOREIGN KEY (`cart_client_user`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_cart_3`
    FOREIGN KEY (`cart_person_ref`)
    REFERENCES `gluttex`.`person` (`id_person`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_cart_5`
    FOREIGN KEY (`cart_invoice`)
    REFERENCES `gluttex`.`invoice` (`invoice_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`comment`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`comment` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`comment` (
  `idcomment` INT NOT NULL AUTO_INCREMENT,
  `comment_owner` INT NULL DEFAULT NULL,
  `comment_content` TEXT NULL DEFAULT NULL,
  `replying_to` INT NULL DEFAULT NULL,
  `comment_creation` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `comment_visibility` TINYINT NULL DEFAULT NULL,
  `comment_edition` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`idcomment`),
  INDEX `fk_comment_1_idx` (`replying_to` ASC) VISIBLE,
  INDEX `fk_comment_2_idx` (`comment_owner` ASC) VISIBLE,
  CONSTRAINT `fk_comment_1`
    FOREIGN KEY (`replying_to`)
    REFERENCES `gluttex`.`comment` (`idcomment`),
  CONSTRAINT `fk_comment_2`
    FOREIGN KEY (`comment_owner`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`comment_reaction`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`comment_reaction` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`comment_reaction` (
  `id_comment_reaction` INT NOT NULL AUTO_INCREMENT,
  `comment_reacting_user` INT NULL DEFAULT NULL,
  `comment_reaction` ENUM('like', 'dislike', 'love', 'helpful', 'not_helpful', 'star') NULL DEFAULT 'like',
  `reacted_on_comment` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_comment_reaction`),
  INDEX `fk_product_reaction_1_idx` (`comment_reacting_user` ASC) VISIBLE,
  INDEX `fk_product_reaction_30_idx` (`reacted_on_comment` ASC) VISIBLE,
  CONSTRAINT `fk_product_reaction_11`
    FOREIGN KEY (`comment_reacting_user`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_product_reaction_30`
    FOREIGN KEY (`reacted_on_comment`)
    REFERENCES `gluttex`.`comment` (`idcomment`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`ingredient`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`ingredient` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`ingredient` (
  `id_ingredient` INT NOT NULL AUTO_INCREMENT,
  `ingredient_quantifier` ENUM('g', 'kg', 'mg', 'L', 'mL', 'pc', 'pkg', 'box', 'bag', 'slice', 'cup') NULL DEFAULT 'pc',
  `ingredient_naming_contribution` INT NULL,
  `ingredient_icon_url` VARCHAR(255) NULL,
  `ingredient_name` VARCHAR(255) NULL,
  PRIMARY KEY (`id_ingredient`),
  INDEX `fk_ingredient_1_idx` (`ingredient_naming_contribution` ASC) VISIBLE,
  CONSTRAINT `fk_ingredient_1`
    FOREIGN KEY (`ingredient_naming_contribution`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`product_category`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`product_category` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`product_category` (
  `id_product_category` INT NOT NULL AUTO_INCREMENT,
  `product_category_naming_ref` INT NULL,
  `product_category_icon` VARCHAR(255) NULL,
  `product_category_name` VARCHAR(255) NULL,
  PRIMARY KEY (`id_product_category`),
  INDEX `fk_product_category_1_idx` (`product_category_naming_ref` ASC) VISIBLE,
  CONSTRAINT `fk_product_category_1`
    FOREIGN KEY (`product_category_naming_ref`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`iproduct`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`iproduct` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`iproduct` (
  `id_iproduct` INT NOT NULL AUTO_INCREMENT,
  `iproduct_barcode` VARCHAR(45) NULL DEFAULT NULL,
  `iproduct_brand` VARCHAR(255) NULL DEFAULT NULL,
  `iproduct_estimated_price` DECIMAL(8,2) NULL DEFAULT '0.00',
  `iproduct_price_currency` VARCHAR(45) NULL DEFAULT 'DZD',
  `iproduct_gluten_status` ENUM('gluten_free', 'contains_gluten', 'may_contain_gluten', 'unknown') NULL DEFAULT 'unknown',
  `iproduct_info_source` VARCHAR(255) NULL DEFAULT NULL,
  `iproduct_last_price_update` DATETIME NULL DEFAULT NULL,
  `iproduct_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `iproduct_last_update` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `iproduct_model_name` VARCHAR(255) NULL DEFAULT NULL,
  `iproduct_image_url` VARCHAR(255) NULL DEFAULT NULL,
  `iproduct_naming_ref` INT NULL DEFAULT NULL,
  `iproduct_info_confidence` DECIMAL(5,4) NULL DEFAULT NULL,
  `iproduct_category_id` INT NULL DEFAULT NULL,
  `iproduct_name` VARCHAR(255) NULL,
  PRIMARY KEY (`id_iproduct`),
  INDEX `fk_iproduct_1_idx` (`iproduct_category_id` ASC) VISIBLE,
  INDEX `fk_iproduct_2_idx` (`iproduct_naming_ref` ASC) VISIBLE,
  CONSTRAINT `fk_iproduct_1`
    FOREIGN KEY (`iproduct_category_id`)
    REFERENCES `gluttex`.`product_category` (`id_product_category`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_iproduct_2`
    FOREIGN KEY (`iproduct_naming_ref`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`location_image`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`location_image` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`location_image` (
  `id_location_image` INT NOT NULL AUTO_INCREMENT,
  `location_image_url` VARCHAR(255) NULL DEFAULT NULL,
  `image_location_ref` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_location_image`),
  INDEX `fk_location_image_1_idx` (`image_location_ref` ASC) VISIBLE,
  CONSTRAINT `fk_location_image_1`
    FOREIGN KEY (`image_location_ref`)
    REFERENCES `gluttex`.`location` (`id_location`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`management_rule`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`management_rule` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`management_rule` (
  `id_management_rule` INT NOT NULL AUTO_INCREMENT,
  `rule_ref_org` INT NULL DEFAULT NULL,
  `rule_ref_provider` INT NULL DEFAULT NULL,
  `rule_ref_user` INT NULL DEFAULT NULL,
  `management_rule_code` INT NULL DEFAULT NULL,
  `management_rule_status` ENUM('PENDING', 'REJECTED', 'SUSPENDED', 'OBSOLETE', 'ACTIVE') NULL DEFAULT 'PENDING',
  `management_rule_expiry` DATETIME NULL DEFAULT NULL,
  PRIMARY KEY (`id_management_rule`),
  INDEX `fk_management_rule_1_idx` (`rule_ref_org` ASC) VISIBLE,
  INDEX `fk_management_rule_2_idx` (`rule_ref_provider` ASC) VISIBLE,
  INDEX `fk_management_rule_3_idx` (`rule_ref_user` ASC) VISIBLE,
  CONSTRAINT `fk_management_rule_1`
    FOREIGN KEY (`rule_ref_org`)
    REFERENCES `gluttex`.`provider_organisation` (`idprovider_organisation`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_management_rule_2`
    FOREIGN KEY (`rule_ref_provider`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`),
  CONSTRAINT `fk_management_rule_3`
    FOREIGN KEY (`rule_ref_user`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`notification`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`notification` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`notification` (
  `id_notification` INT NOT NULL AUTO_INCREMENT,
  `notification_code` ENUM('order_placed', 'order_confirmed', 'order_processing', 'order_shipped', 'order_delivered', 'order_cancelled', 'order_refunded', 'order_status_updated', 'payment_received', 'payment_failed', 'payment_refunded', 'payment_pending', 'invoice_created', 'invoice_paid', 'invoice_overdue', 'invoice_cancelled', 'deposit_received', 'deposit_confirmed', 'cart_abandoned', 'cart_reminder', 'cart_created', 'cart_updated', 'product_added', 'product_updated', 'product_deleted', 'product_stock_low', 'product_stock_restocked', 'product_price_changed', 'product_reviewed', 'product_rating', 'product_featured', 'product_discount', 'product_back_in_stock', 'product_available', 'product_out_of_stock', 'supplier_created', 'supplier_updated', 'supplier_deleted', 'supplier_verified', 'supplier_suspended', 'supplier_approved', 'provider_created', 'provider_updated', 'provider_deleted', 'provider_verified', 'provider_suspended', 'provider_approved', 'role_invitation', 'role_invitation_accepted', 'role_invitation_rejected', 'role_invitation_expired', 'role_assigned', 'role_removed', 'role_updated', 'role_changed', 'permission_granted', 'permission_revoked', 'user_role_changed', 'recipe_added', 'recipe_updated', 'recipe_deleted', 'recipe_liked', 'recipe_commented', 'recipe_featured', 'recipe_shared', 'recipe_saved', 'user_registered', 'user_verified', 'user_password_changed', 'user_profile_updated', 'user_logged_in', 'user_logged_out', 'user_suspended', 'user_reactivated', 'user_deleted', 'user_email_verified', 'user_phone_verified', 'user_account_approved', 'user_account_rejected', 'user_account_pending', 'subscription_created', 'subscription_renewed', 'subscription_expired', 'subscription_cancelled', 'subscription_payment_failed', 'subscription_upgraded', 'subscription_downgraded', 'subscription_trial_ending', 'subscription_payment_success', 'delivery_created', 'delivery_confirmed', 'delivery_shipped', 'delivery_in_transit', 'delivery_out_for_delivery', 'delivery_delivered', 'delivery_failed', 'delivery_cancelled', 'delivery_returned', 'delivery_refunded', 'delivery_status_updated', 'delivery_scheduled', 'delivery_delayed', 'service_created', 'service_updated', 'service_deleted', 'service_scheduled', 'service_completed', 'service_cancelled', 'service_reminder', 'service_rescheduled', 'service_reviewed', 'service_rating', 'service_confirmed', 'service_started', 'service_finished', 'comment_added', 'comment_replied', 'comment_mentioned', 'comment_liked', 'comment_deleted', 'comment_reported', 'comment_approved', 'comment_spam', 'reaction_received', 'reaction_liked', 'reaction_loved', 'reaction_rated', 'reaction_disliked', 'report_created', 'report_processed', 'report_resolved', 'report_closed', 'report_rejected', 'report_escalated', 'wallet_credited', 'wallet_debited', 'wallet_balance_low', 'wallet_balance_updated', 'wallet_created', 'wallet_activated', 'wallet_suspended', 'wallet_closed', 'wallet_pending_verification', 'wallet_verified', 'wallet_transaction_completed', 'money_transaction_initiated', 'money_transaction_completed', 'money_transaction_failed', 'money_transaction_reversed', 'money_transaction_cancelled', 'money_transaction_refunded', 'money_transaction_pending', 'org_invitation', 'org_invitation_accepted', 'org_invitation_rejected', 'org_member_added', 'org_member_removed', 'org_member_left', 'org_created', 'org_updated', 'org_deleted', 'org_activated', 'org_suspended', 'team_invitation', 'team_invitation_accepted', 'team_invitation_rejected', 'team_member_added', 'team_member_removed', 'team_created', 'team_updated', 'team_deleted', 'welcome', 'onboarding_complete', 'profile_updated', 'account_created', 'account_suspended', 'account_reactivated', 'account_deleted', 'settings_updated', 'preferences_updated', 'security_alert', 'security_alert_unusual_login', 'security_alert_password_change', 'security_alert_two_factor', 'security_alert_device_recognized', 'security_alert_device_unknown', 'system_announcement', 'maintenance_scheduled', 'maintenance_completed', 'new_version_available', 'feature_available', 'newsletter', 'promotional_offer', 'discount_available', 'coupon_received', 'loyalty_points_earned', 'loyalty_points_used', 'birthday_greeting', 'milestone_achieved', 'feedback_request', 'survey_request', 'upcoming_event', 'event_reminder', 'event_cancelled', 'event_postponed', 'notification_preference_updated', 'notification_settings_changed', 'unread_messages', 'new_message_received', 'message_read', 'message_sent', 'message_failed', 'conversation_started', 'conversation_ended', 'conversation_updated', 'connection_request', 'connection_approved', 'connection_rejected', 'connection_removed', 'connection_suggested', 'follow_request', 'follow_request_accepted', 'follow_request_rejected', 'follow_started', 'follow_ended', 'share_received', 'share_accepted', 'share_rejected', 'share_expired', 'review_requested', 'review_submitted', 'review_updated', 'review_deleted', 'review_responded', 'appointment_scheduled', 'appointment_confirmed', 'appointment_rescheduled', 'appointment_cancelled', 'appointment_reminder', 'appointment_completed', 'appointment_no_show', 'task_assigned', 'task_updated', 'task_completed', 'task_overdue', 'task_deleted', 'task_reassigned', 'document_uploaded', 'document_shared', 'document_updated', 'document_deleted', 'document_expired', 'document_approved', 'document_rejected', 'document_signed', 'compliance_alert', 'compliance_approved', 'compliance_rejected', 'compliance_updated', 'compliance_reminder', 'compliance_due', 'compliance_overdue', 'backup_completed', 'backup_failed', 'backup_restored', 'backup_scheduled', 'integration_connected', 'integration_disconnected', 'integration_error', 'integration_success', 'integration_sync_completed', 'integration_sync_failed') NULL DEFAULT NULL,
  `notification_params` TEXT NULL DEFAULT NULL,
  `notification_user_ref` INT NULL DEFAULT NULL,
  `notification_created_at` DATETIME NULL DEFAULT NULL,
  `notification_read_at` DATETIME NULL DEFAULT NULL,
  PRIMARY KEY (`id_notification`),
  INDEX `fk_notification_1_idx` (`notification_user_ref` ASC) VISIBLE,
  CONSTRAINT `fk_notification_1`
    FOREIGN KEY (`notification_user_ref`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`product`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`product` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`product` (
  `id_product` INT NOT NULL AUTO_INCREMENT,
  `product_name` VARCHAR(45) NULL DEFAULT NULL,
  `product_brand` VARCHAR(45) NULL DEFAULT NULL,
  `product_provider_id` INT NULL DEFAULT NULL,
  `product_category_id` INT NULL DEFAULT NULL,
  `product_barcode` VARCHAR(45) NULL DEFAULT NULL,
  `last_updated` DATETIME NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `created` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `product_description` VARCHAR(300) NULL DEFAULT NULL,
  `product_price` DOUBLE NULL DEFAULT 0.0,
  `product_base_price` DOUBLE NULL DEFAULT 0.0,
  `product_quantity` INT NULL DEFAULT 0,
  `product_reserved_quantity` INT NULL DEFAULT 0,
  `product_quantifier` VARCHAR(45) NULL DEFAULT NULL,
  `product_owner` INT NULL DEFAULT NULL,
  `product_origin_id` INT NULL DEFAULT NULL,
  `product_visibility` ENUM('VISIBLE', 'HIDDEN') NULL DEFAULT 'VISIBLE',
  PRIMARY KEY (`id_product`),
  INDEX `fk_product_1_idx` (`product_provider_id` ASC) VISIBLE,
  INDEX `fk_product_2_idx` (`product_category_id` ASC) VISIBLE,
  INDEX `fk_product_3_idx` (`product_owner` ASC) VISIBLE,
  INDEX `fk_product_4_idx` (`product_origin_id` ASC) VISIBLE,
  CONSTRAINT `fk_product_1`
    FOREIGN KEY (`product_provider_id`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_product_2`
    FOREIGN KEY (`product_category_id`)
    REFERENCES `gluttex`.`product_category` (`id_product_category`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_product_3`
    FOREIGN KEY (`product_owner`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_product_4`
    FOREIGN KEY (`product_origin_id`)
    REFERENCES `gluttex`.`iproduct` (`id_iproduct`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`placed_order`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`placed_order` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`placed_order` (
  `id_placed_order` INT NOT NULL AUTO_INCREMENT,
  `order_discount` DOUBLE NULL DEFAULT NULL,
  `total_price` DOUBLE NULL DEFAULT NULL,
  `ordering_user_id` INT NULL DEFAULT NULL,
  `placed_order_location_ref` INT NULL DEFAULT NULL,
  `placed_order_state` ENUM("PENDING", "PROCESSING", "SHIPPED", "DELIVERED", "CANCELLED", "REFUNDED") NULL DEFAULT 'PENDING',
  `placed_order_last_mod` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `placed_order_receipt_ref` INT NULL,
  `placed_order_creation` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `placed_order_invoice` INT NULL,
  PRIMARY KEY (`id_placed_order`),
  INDEX `fk_placed_order_1_idx` (`ordering_user_id` ASC) VISIBLE,
  INDEX `fk_placed_order_2_idx` (`placed_order_location_ref` ASC) VISIBLE,
  INDEX `fk_placed_order_3_idx` (`placed_order_invoice` ASC) VISIBLE,
  CONSTRAINT `fk_placed_order_1`
    FOREIGN KEY (`ordering_user_id`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_placed_order_2`
    FOREIGN KEY (`placed_order_location_ref`)
    REFERENCES `gluttex`.`location` (`id_location`),
  CONSTRAINT `fk_placed_order_3`
    FOREIGN KEY (`placed_order_invoice`)
    REFERENCES `gluttex`.`invoice` (`invoice_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`ordered_item`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`ordered_item` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`ordered_item` (
  `id_ordered_item` INT NOT NULL AUTO_INCREMENT,
  `ordered_product_id` INT NULL DEFAULT NULL,
  `ordered_quantity` INT NULL DEFAULT 0,
  `reserved_quantity` INT NULL DEFAULT 0,
  `applied_vat` DOUBLE NULL DEFAULT NULL,
  `order_ref` INT NULL DEFAULT NULL,
  `unit_price` DOUBLE NULL DEFAULT NULL,
  `product_discount` DOUBLE NULL DEFAULT NULL,
  `ordered_item_cart_ref` INT NULL,
  `ordered_item_delivery_status` ENUM('pending', 'processing', 'shipped', 'delivered', 'cancelled', 'returned', 'partial') NULL DEFAULT 'pending',
  `ordered_item_delivery_fee` DOUBLE NULL DEFAULT 0.0,
  `ordered_item_last_mod` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `placed_order_creation` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id_ordered_item`),
  INDEX `fk_ordered_item_1_idx` (`ordered_product_id` ASC) VISIBLE,
  INDEX `fk_ordered_item_3_idx` (`order_ref` ASC) VISIBLE,
  INDEX `fk_ordered_item_2_idx` (`ordered_item_cart_ref` ASC) VISIBLE,
  CONSTRAINT `fk_ordered_item_1`
    FOREIGN KEY (`ordered_product_id`)
    REFERENCES `gluttex`.`product` (`id_product`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_ordered_item_3`
    FOREIGN KEY (`order_ref`)
    REFERENCES `gluttex`.`placed_order` (`id_placed_order`),
  CONSTRAINT `fk_ordered_item_2`
    FOREIGN KEY (`ordered_item_cart_ref`)
    REFERENCES `gluttex`.`cart` (`cart_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`provided_service`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`provided_service` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`provided_service` (
  `provided_service_id` INT NOT NULL AUTO_INCREMENT,
  `provided_service_name` VARCHAR(255) NULL,
  `provided_service_description` TEXT NULL DEFAULT NULL,
  `provided_service_category_id` INT NULL,
  `provided_service_product_provider_id` INT NULL COMMENT 'References i_product_provider.id_product_provider',
  `provided_service_base_price` DECIMAL(15,4) NULL,
  `provided_service_final_price` DECIMAL(15,4) NULL,
  `provided_service_actual_duration` DECIMAL(10,2) NULL,
  `provided_service_is_active` TINYINT(1) NULL DEFAULT '1',
  `provided_service_pricing_config` JSON NULL,
  `provided_service_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `provided_service_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `provided_service_deleted_at` TIMESTAMP NULL DEFAULT NULL,
  PRIMARY KEY (`provided_service_id`),
  INDEX `idx_provided_service_category` (`provided_service_category_id` ASC) VISIBLE,
  INDEX `idx_provided_service_provider` (`provided_service_product_provider_id` ASC) VISIBLE,
  INDEX `idx_provided_service_name` (`provided_service_name` ASC) VISIBLE,
  INDEX `idx_provided_service_active` (`provided_service_is_active` ASC, `provided_service_deleted_at` ASC) VISIBLE,
  INDEX `idx_provided_service_price_range` (`provided_service_base_price` ASC, `provided_service_final_price` ASC) VISIBLE,
  INDEX `idx_provided_service_created_at` (`provided_service_created_at` ASC) VISIBLE,
  CONSTRAINT `provided_service_ibfk_1`
    FOREIGN KEY (`provided_service_category_id`)
    REFERENCES `gluttex`.`provided_service_category` (`provided_service_category_id`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE,
  CONSTRAINT `provided_service_ibfk_2`
    FOREIGN KEY (`provided_service_product_provider_id`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`ordered_service`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`ordered_service` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`ordered_service` (
  `ordered_service_id` INT NOT NULL AUTO_INCREMENT,
  `ordered_service_cart_id` INT NULL,
  `ordered_service_service_id` INT NULL COMMENT 'References provided_service',
  `ordered_service_quantity` INT NULL DEFAULT '1',
  `ordered_service_unit_price` DECIMAL(15,4) NULL,
  `ordered_service_total_price` DECIMAL(15,4) NULL,
  `ordered_service_notes` TEXT NULL DEFAULT NULL,
  `ordered_service_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `ordered_service_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `ordered_service_scheduled_at` TIMESTAMP NULL,
  `ordered_service_delivery_fee` DOUBLE NULL DEFAULT 0.0,
  `ordered_service_delivery_status` ENUM('pending', 'processing', 'scheduled', 'in_progress', 'completed', 'cancelled', 'no_show') NULL DEFAULT 'pending',
  PRIMARY KEY (`ordered_service_id`),
  INDEX `idx_cart` (`ordered_service_cart_id` ASC) VISIBLE,
  INDEX `idx_service` (`ordered_service_service_id` ASC) VISIBLE,
  CONSTRAINT `ordered_service_ibfk_1`
    FOREIGN KEY (`ordered_service_cart_id`)
    REFERENCES `gluttex`.`cart` (`cart_id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,
  CONSTRAINT `ordered_service_ibfk_2`
    FOREIGN KEY (`ordered_service_service_id`)
    REFERENCES `gluttex`.`provided_service` (`provided_service_id`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`organisation_image`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`organisation_image` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`organisation_image` (
  `id_org_image` INT NOT NULL AUTO_INCREMENT,
  `org_image_url` VARCHAR(255) NULL DEFAULT NULL,
  `org_ref_id` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_org_image`),
  INDEX `fk_organisation_image_1_idx` (`org_ref_id` ASC) VISIBLE,
  CONSTRAINT `fk_organisation_image_1`
    FOREIGN KEY (`org_ref_id`)
    REFERENCES `gluttex`.`provider_organisation` (`idprovider_organisation`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`patient`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`patient` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`patient` (
  `id_patient` INT NOT NULL AUTO_INCREMENT,
  `patient_person_id` INT NULL DEFAULT NULL,
  `patient_disease_severity` ENUM('mild', 'moderate', 'severe', 'critical') NULL DEFAULT 'mild',
  PRIMARY KEY (`id_patient`),
  INDEX `fk_patient_1_idx` (`patient_person_id` ASC) VISIBLE,
  CONSTRAINT `fk_patient_1`
    FOREIGN KEY (`patient_person_id`)
    REFERENCES `gluttex`.`person` (`id_person`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`payment`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`payment` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`payment` (
  `payment_id` INT NOT NULL AUTO_INCREMENT,
  `payment_invoice_id` INT NULL,
  `payment_amount` DECIMAL(15,4) NULL,
  `payment_method` ENUM('cash', 'card', 'bank_transfer', 'mobile_money', 'crypto', 'deposit', 'wallet', 'check') NULL DEFAULT 'cash' COMMENT 'cash, card, bank, mobile',
  `payment_status` ENUM('pending', 'processing', 'completed', 'partial', 'failed', 'cancelled') NULL DEFAULT 'pending',
  `payment_reference` VARCHAR(255) NULL DEFAULT NULL COMMENT 'Bank or transaction reference',
  `payment_notes` TEXT NULL DEFAULT NULL,
  `payment_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `payment_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `payment_type` ENUM('deposit', 'payment', 'refund') NULL,
  PRIMARY KEY (`payment_id`),
  INDEX `idx_invoice` (`payment_invoice_id` ASC) VISIBLE,
  INDEX `idx_status` (`payment_status` ASC) VISIBLE,
  CONSTRAINT `payment_ibfk_1`
    FOREIGN KEY (`payment_invoice_id`)
    REFERENCES `gluttex`.`invoice` (`invoice_id`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`symptom`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`symptom` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`symptom` (
  `id_symptom` INT NOT NULL AUTO_INCREMENT,
  `symptom_naming_ref` INT NULL DEFAULT NULL,
  `symptom_icon_url` VARCHAR(255) NULL,
  PRIMARY KEY (`id_symptom`),
  INDEX `fk_symptom_1_idx` (`symptom_naming_ref` ASC) VISIBLE,
  CONSTRAINT `fk_symptom_1`
    FOREIGN KEY (`symptom_naming_ref`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`symptoms_occurence`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`symptoms_occurence` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`symptoms_occurence` (
  `id_symptoms_occurence` INT NOT NULL AUTO_INCREMENT,
  `symptoms_occurence_submission_time` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `symptoms_occurence_reason` VARCHAR(300) NULL DEFAULT NULL,
  `reason_date` TIMESTAMP NULL DEFAULT NULL,
  `symptoms_occurence_ref_patient` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_symptoms_occurence`),
  INDEX `fk_symptoms_causality_1_idx` (`symptoms_occurence_ref_patient` ASC) VISIBLE,
  CONSTRAINT `fk_symptoms_causality_1`
    FOREIGN KEY (`symptoms_occurence_ref_patient`)
    REFERENCES `gluttex`.`patient` (`id_patient`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`presented_symptom`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`presented_symptom` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`presented_symptom` (
  `id_presented_symptom` INT NOT NULL AUTO_INCREMENT,
  `presented_symptom_ref_symptoms_occurence` INT NULL DEFAULT NULL,
  `presented_symptom_ref_symptom` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_presented_symptom`),
  INDEX `fk_presented_symptom_1_idx` (`presented_symptom_ref_symptom` ASC) VISIBLE,
  INDEX `fk_presented_symptom_2_idx` (`presented_symptom_ref_symptoms_occurence` ASC) VISIBLE,
  CONSTRAINT `fk_presented_symptom_1`
    FOREIGN KEY (`presented_symptom_ref_symptom`)
    REFERENCES `gluttex`.`symptom` (`id_symptom`),
  CONSTRAINT `fk_presented_symptom_2`
    FOREIGN KEY (`presented_symptom_ref_symptoms_occurence`)
    REFERENCES `gluttex`.`symptoms_occurence` (`id_symptoms_occurence`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`product_image`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`product_image` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`product_image` (
  `id_product_image` INT NOT NULL AUTO_INCREMENT,
  `product_image_url` VARCHAR(255) NULL DEFAULT NULL,
  `product_ref_id` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_product_image`),
  INDEX `fk_product_image_1_idx` (`product_ref_id` ASC) VISIBLE,
  CONSTRAINT `fk_product_image_1`
    FOREIGN KEY (`product_ref_id`)
    REFERENCES `gluttex`.`product` (`id_product`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`product_reaction`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`product_reaction` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`product_reaction` (
  `id_product_reaction` INT NOT NULL AUTO_INCREMENT,
  `product_reacting_user` INT NULL DEFAULT NULL,
  `product_reaction` ENUM('like', 'dislike', 'love', 'helpful', 'not_helpful', 'star') NULL DEFAULT 'like',
  `reacted_on_product` INT NULL DEFAULT NULL,
  `product_reaction_value` FLOAT NULL DEFAULT '0',
  PRIMARY KEY (`id_product_reaction`),
  INDEX `fk_product_reaction_1_idx` (`product_reacting_user` ASC) VISIBLE,
  INDEX `fk_product_reaction_3_idx` (`reacted_on_product` ASC) VISIBLE,
  CONSTRAINT `fk_product_reaction_1`
    FOREIGN KEY (`product_reacting_user`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`),
  CONSTRAINT `fk_product_reaction_3`
    FOREIGN KEY (`reacted_on_product`)
    REFERENCES `gluttex`.`product` (`id_product`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`provider_image`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`provider_image` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`provider_image` (
  `id_provider_image` INT NOT NULL AUTO_INCREMENT,
  `provider_image_url` VARCHAR(255) NULL DEFAULT NULL,
  `provider_ref_id` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_provider_image`),
  INDEX `fk_provider_image_1_idx` (`provider_ref_id` ASC) VISIBLE,
  CONSTRAINT `fk_provider_image_1`
    FOREIGN KEY (`provider_ref_id`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`provider_reaction`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`provider_reaction` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`provider_reaction` (
  `id_provider_reaction` INT NOT NULL AUTO_INCREMENT,
  `provider_reacting_user` INT NULL DEFAULT NULL,
  `provider_reaction` ENUM('like', 'dislike', 'love', 'helpful', 'not_helpful', 'star') NULL DEFAULT 'like',
  `reacted_on_provider` INT NULL DEFAULT NULL,
  `provider_reaction_value` FLOAT NULL DEFAULT '0',
  PRIMARY KEY (`id_provider_reaction`),
  INDEX `fk_product_reaction_1_idx` (`provider_reacting_user` ASC) VISIBLE,
  INDEX `fk_product_reaction_31_idx` (`reacted_on_provider` ASC) VISIBLE,
  CONSTRAINT `fk_product_reaction_12`
    FOREIGN KEY (`provider_reacting_user`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`),
  CONSTRAINT `fk_product_reaction_31`
    FOREIGN KEY (`reacted_on_provider`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`recipe_category`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`recipe_category` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`recipe_category` (
  `id_recipe_category` INT NOT NULL AUTO_INCREMENT,
  `recipe_category_naming` INT NULL DEFAULT NULL,
  `recipe_category_icon_url` VARCHAR(255) NULL,
  `recipe_category_name` VARCHAR(255) NULL,
  PRIMARY KEY (`id_recipe_category`),
  INDEX `fk_recipe_category_1_idx` (`recipe_category_naming` ASC) VISIBLE,
  CONSTRAINT `fk_recipe_category_1`
    FOREIGN KEY (`recipe_category_naming`)
    REFERENCES `gluttex`.`naming_contribution` (`id_naming_contribution`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`recipe`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`recipe` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`recipe` (
  `id_recipe` INT NOT NULL AUTO_INCREMENT,
  `recipe_owner_id` INT NULL DEFAULT NULL,
  `recipe_category_id` INT NULL DEFAULT NULL,
  `recipe_preparation_time` VARCHAR(45) NULL DEFAULT NULL,
  `recipe_instructions` TEXT NULL DEFAULT NULL,
  `recipe_name` VARCHAR(255) NULL DEFAULT NULL,
  `recipe_description` VARCHAR(300) NULL DEFAULT NULL,
  `recipe_creation` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `recipe_last_updated` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id_recipe`),
  INDEX `fk_recipe_1_idx` (`recipe_owner_id` ASC) VISIBLE,
  INDEX `fk_recipe_2_idx` (`recipe_category_id` ASC) VISIBLE,
  CONSTRAINT `fk_recipe_1`
    FOREIGN KEY (`recipe_owner_id`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`),
  CONSTRAINT `fk_recipe_2`
    FOREIGN KEY (`recipe_category_id`)
    REFERENCES `gluttex`.`recipe_category` (`id_recipe_category`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`recipe_contains_ingredient`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`recipe_contains_ingredient` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`recipe_contains_ingredient` (
  `idrecipe_contains_ingredient_id` INT NOT NULL AUTO_INCREMENT,
  `containing_recipe_id` INT NULL DEFAULT NULL,
  `contained_ingredient_id` INT NULL DEFAULT NULL,
  `contained_quantity` VARCHAR(45) NULL DEFAULT NULL,
  PRIMARY KEY (`idrecipe_contains_ingredient_id`),
  INDEX `fk_recipe_contains_ingredient_1_idx` (`containing_recipe_id` ASC) VISIBLE,
  INDEX `fk_recipe_contains_ingredient_2_idx` (`contained_ingredient_id` ASC) VISIBLE,
  CONSTRAINT `fk_recipe_contains_ingredient_1`
    FOREIGN KEY (`containing_recipe_id`)
    REFERENCES `gluttex`.`recipe` (`id_recipe`)
    ON DELETE RESTRICT
    ON UPDATE RESTRICT,
  CONSTRAINT `fk_recipe_contains_ingredient_2`
    FOREIGN KEY (`contained_ingredient_id`)
    REFERENCES `gluttex`.`ingredient` (`id_ingredient`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`recipe_image`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`recipe_image` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`recipe_image` (
  `id_recipe_image` INT NOT NULL AUTO_INCREMENT,
  `recipe_image_url` VARCHAR(255) NULL DEFAULT NULL,
  `recipe_ref_id` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_recipe_image`),
  INDEX `fk_recipe_image_1_idx` (`recipe_ref_id` ASC) VISIBLE,
  CONSTRAINT `fk_recipe_image_1`
    FOREIGN KEY (`recipe_ref_id`)
    REFERENCES `gluttex`.`recipe` (`id_recipe`))
ENGINE = InnoDB
AUTO_INCREMENT = 1
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`recipe_reaction`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`recipe_reaction` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`recipe_reaction` (
  `id_recipe_reaction` INT NOT NULL AUTO_INCREMENT,
  `recipe_reacting_user` INT NULL DEFAULT NULL,
  `recipe_reaction` ENUM('like', 'dislike', 'love', 'helpful', 'not_helpful', 'star') NULL DEFAULT 'like',
  `reacted_on_recipe` INT NULL DEFAULT NULL,
  PRIMARY KEY (`id_recipe_reaction`),
  INDEX `fk_product_reaction_1_idx` (`recipe_reacting_user` ASC) VISIBLE,
  INDEX `fk_recipe_reaction_1_idx` (`reacted_on_recipe` ASC) VISIBLE,
  CONSTRAINT `fk_product_reaction_10`
    FOREIGN KEY (`recipe_reacting_user`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`),
  CONSTRAINT `fk_recipe_reaction_1`
    FOREIGN KEY (`reacted_on_recipe`)
    REFERENCES `gluttex`.`recipe` (`id_recipe`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`report`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`report` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`report` (
  `id_report` INT NOT NULL AUTO_INCREMENT,
  `report_text` TEXT NULL DEFAULT NULL,
  `report_owner` INT NULL DEFAULT NULL,
  `report_type` ENUM('spam', 'abuse', 'harassment', 'offensive_content', 'inappropriate_content', 'hate_speech', 'bullying', 'threatening', 'violent_content', 'adult_content', 'misinformation', 'fake_news', 'scam', 'fraud', 'phishing', 'malware', 'virus', 'security_issue', 'privacy_issue', 'data_breach', 'product_misrepresentation', 'counterfeit_product', 'product_safety_concern', 'expired_product', 'damaged_product', 'wrong_product', 'product_quality_issue', 'product_quantity_issue', 'product_price_issue', 'product_availability_issue', 'provider_fraud', 'provider_scam', 'provider_poor_service', 'provider_unresponsive', 'provider_misleading', 'provider_fake_reviews', 'provider_duplicate', 'provider_suspended_activity', 'provider_unauthorized_charges', 'fake_account', 'impersonation', 'identity_theft', 'user_harassment', 'user_abuse', 'user_spam', 'user_misconduct', 'user_violation', 'user_underage', 'user_inappropriate_behavior', 'recipe_inaccurate', 'recipe_unsafe', 'recipe_allergen_issue', 'recipe_misleading', 'recipe_copycat', 'recipe_fake', 'recipe_offensive', 'recipe_dangerous_ingredients', 'recipe_incorrect_instructions', 'service_misrepresentation', 'service_unprofessional', 'service_cancelled_no_show', 'service_quality_issue', 'service_safety_concern', 'service_incomplete', 'service_overcharged', 'service_unavailable', 'service_fake', 'delivery_misrepresentation', 'delivery_unprofessional', 'delivery_cancelled_no_show', 'delivery_quality_issue', 'delivery_safety_concern', 'delivery_incomplete', 'delivery_overcharged', 'delivery_unavailable', 'delivery_fake', 'order_fraud', 'order_scam', 'order_dispute', 'order_missing_items', 'order_wrong_items', 'order_delivery_issue', 'order_payment_issue', 'order_refund_issue', 'order_cancellation_issue', 'payment_fraud', 'payment_unauthorized', 'payment_overcharged', 'payment_double_charged', 'payment_refund_issue', 'payment_missing', 'financial_fraud', 'financial_misrepresentation', 'financial_unauthorized_charges', 'financial_discrepancy', 'financial_missing_transaction', 'financial_duplicate_transaction', 'review_fake', 'review_bought', 'review_offensive', 'review_spam', 'review_irrelevant', 'review_duplicate', 'review_misleading', 'review_inappropriate', 'comment_spam', 'comment_abuse', 'comment_harassment', 'comment_offensive', 'comment_inappropriate', 'comment_hate_speech', 'comment_fake', 'payment_dispute', 'payment_reversal', 'payment_chargeback', 'payment_refund_request', 'payment_unauthorized_charge', 'payment_billing_error', 'system_bug', 'system_error', 'system_performance_issue', 'system_security_issue', 'system_data_issue', 'system_integration_issue', 'system_ui_issue', 'system_feature_request', 'system_enhancement', 'copyright_infringement', 'trademark_infringement', 'intellectual_property_violation', 'defamation', 'libel', 'slander', 'confidential_info_leak', 'trade_secret_violation', 'terms_violation', 'policy_violation', 'community_guidelines_violation', 'legal_issue', 'support_request', 'support_issue', 'support_complaint', 'support_feedback', 'support_suggestion', 'support_compliment', 'support_bug_report', 'support_feature_request', 'other', 'general_complaint', 'general_feedback', 'general_inquiry', 'general_issue') NULL DEFAULT 'other',
  `report_appreciation` ENUM('valid', 'plausible', 'invalid') NULL,
  `report_appreciation_value` FLOAT NULL,
  PRIMARY KEY (`id_report`),
  INDEX `fk_report_1_idx` (`report_owner` ASC) VISIBLE,
  CONSTRAINT `fk_report_1`
    FOREIGN KEY (`report_owner`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`serology_indicator`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`serology_indicator` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`serology_indicator` (
  `id_serology_indicator` INT NOT NULL AUTO_INCREMENT,
  `serology_indicator_name` VARCHAR(45) NULL DEFAULT NULL,
  `serology_indicator_desc` VARCHAR(300) NULL DEFAULT NULL,
  PRIMARY KEY (`id_serology_indicator`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`serology`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`serology` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`serology` (
  `id_serology` INT NOT NULL AUTO_INCREMENT,
  `indicator_id` INT NULL DEFAULT NULL,
  `serology_date` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `patient_id` INT NULL DEFAULT NULL,
  `indicator_value` VARCHAR(45) NULL DEFAULT NULL,
  PRIMARY KEY (`id_serology`),
  INDEX `fk_diagnosis_1_idx` (`patient_id` ASC) VISIBLE,
  INDEX `fk_serology_1_idx` (`indicator_id` ASC) VISIBLE,
  CONSTRAINT `fk_diagnosis_1`
    FOREIGN KEY (`patient_id`)
    REFERENCES `gluttex`.`patient` (`id_patient`),
  CONSTRAINT `fk_serology_1`
    FOREIGN KEY (`indicator_id`)
    REFERENCES `gluttex`.`serology_indicator` (`id_serology_indicator`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`service_package`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`service_package` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`service_package` (
  `service_package_id` INT NOT NULL AUTO_INCREMENT,
  `service_package_name` VARCHAR(255) NULL,
  `service_package_description` TEXT NULL DEFAULT NULL,
  `service_package_product_provider_id` INT NULL,
  `service_package_price` DECIMAL(15,4) NULL,
  `service_package_discount_percentage` DECIMAL(5,2) NULL DEFAULT NULL,
  `service_package_is_active` TINYINT(1) NULL DEFAULT '1',
  `service_package_valid_from` DATE NULL DEFAULT NULL,
  `service_package_valid_to` DATE NULL DEFAULT NULL,
  `service_package_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `service_package_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`service_package_id`),
  INDEX `idx_service_package_provider` (`service_package_product_provider_id` ASC) VISIBLE,
  INDEX `idx_service_package_active` (`service_package_is_active` ASC) VISIBLE,
  INDEX `idx_service_package_validity` (`service_package_valid_from` ASC, `service_package_valid_to` ASC) VISIBLE,
  CONSTRAINT `service_package_ibfk_1`
    FOREIGN KEY (`service_package_product_provider_id`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`)
    ON DELETE RESTRICT
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`service_package_item`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`service_package_item` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`service_package_item` (
  `service_package_item_id` INT NOT NULL AUTO_INCREMENT,
  `service_package_item_package_id` INT NULL,
  `service_package_item_service_id` INT NULL,
  `service_package_item_sequence_order` INT NULL DEFAULT '1',
  `service_package_item_quantity` INT NULL DEFAULT '1',
  `service_package_item_notes` TEXT NULL DEFAULT NULL,
  PRIMARY KEY (`service_package_item_id`),
  UNIQUE INDEX `uk_package_service` (`service_package_item_package_id` ASC, `service_package_item_service_id` ASC) VISIBLE,
  INDEX `idx_service_package_item_service` (`service_package_item_service_id` ASC) VISIBLE,
  CONSTRAINT `service_package_item_ibfk_1`
    FOREIGN KEY (`service_package_item_package_id`)
    REFERENCES `gluttex`.`service_package` (`service_package_id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,
  CONSTRAINT `service_package_item_ibfk_2`
    FOREIGN KEY (`service_package_item_service_id`)
    REFERENCES `gluttex`.`provided_service` (`provided_service_id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`service_resource_requirement`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`service_resource_requirement` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`service_resource_requirement` (
  `service_resource_requirement_id` INT NOT NULL AUTO_INCREMENT,
  `service_resource_requirement_service_id` INT NULL,
  `service_resource_requirement_name` VARCHAR(255) NULL,
  `service_resource_requirement_type` VARCHAR(100) NULL DEFAULT NULL,
  `service_resource_requirement_quantity` INT NULL DEFAULT '1',
  `service_resource_requirement_cost_per_unit` DECIMAL(15,4) NULL DEFAULT NULL,
  `service_resource_requirement_is_consumable` TINYINT(1) NULL DEFAULT '0',
  `service_resource_requirement_notes` TEXT NULL DEFAULT NULL,
  `service_resource_requirement_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `service_resource_requirement_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `service_resource_requirement_product_ref` INT NULL,
  PRIMARY KEY (`service_resource_requirement_id`),
  INDEX `idx_resource_requirement_service` (`service_resource_requirement_service_id` ASC) VISIBLE,
  INDEX `idx_resource_requirement_name` (`service_resource_requirement_name` ASC) VISIBLE,
  INDEX `idx_resource_requirement_type` (`service_resource_requirement_type` ASC) VISIBLE,
  INDEX `fk_service_resource_requirement_1_idx` (`service_resource_requirement_product_ref` ASC) VISIBLE,
  CONSTRAINT `service_resource_requirement_ibfk_1`
    FOREIGN KEY (`service_resource_requirement_service_id`)
    REFERENCES `gluttex`.`provided_service` (`provided_service_id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,
  CONSTRAINT `fk_service_resource_requirement_1`
    FOREIGN KEY (`service_resource_requirement_product_ref`)
    REFERENCES `gluttex`.`product` (`id_product`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`service_staff_requirement`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`service_staff_requirement` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`service_staff_requirement` (
  `service_staff_requirement_id` INT NOT NULL AUTO_INCREMENT,
  `service_staff_requirement_service_id` INT NULL,
  `service_staff_requirement_role` INT NULL,
  `service_staff_requirement_min_count` INT NULL DEFAULT '1',
  `service_staff_requirement_max_count` INT NULL DEFAULT NULL,
  `service_staff_requirement_hourly_rate` DECIMAL(15,4) NULL DEFAULT NULL,
  `service_staff_requirement_allocated_hours` DECIMAL(5,2) NULL DEFAULT NULL,
  `service_staff_requirement_notes` TEXT NULL DEFAULT NULL,
  `service_staff_requirement_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `service_staff_requirement_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`service_staff_requirement_id`),
  INDEX `idx_service_staff_requirement_service` (`service_staff_requirement_service_id` ASC) VISIBLE,
  INDEX `idx_service_staff_requirement_role` (`service_staff_requirement_role` ASC) VISIBLE,
  CONSTRAINT `service_staff_requirement_ibfk_1`
    FOREIGN KEY (`service_staff_requirement_service_id`)
    REFERENCES `gluttex`.`provided_service` (`provided_service_id`)
    ON DELETE CASCADE
    ON UPDATE CASCADE,
  CONSTRAINT `fk_service_staff_requirement_1`
    FOREIGN KEY (`service_staff_requirement_role`)
    REFERENCES `gluttex`.`staff_role` (`id_staff_role`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB
DEFAULT CHARACTER SET = utf8mb3;


-- -----------------------------------------------------
-- Table `gluttex`.`service_contribution`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`service_contribution` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`service_contribution` (
  `id_service_contribution` INT NOT NULL AUTO_INCREMENT,
  `service_contribution_duration` VARCHAR(45) NULL,
  `service_contribution_price` FLOAT NULL,
  `service_contribution_currency` VARCHAR(45) NULL,
  `service_contribution_org_ref` INT NULL,
  `service_contribution_user_ref` INT NULL,
  `service_contribution_person_ref` INT NULL,
  `service_ref` INT NULL,
  `service_contribution_start` TIMESTAMP NULL,
  `service_contribution_provider_ref` INT NULL,
  PRIMARY KEY (`id_service_contribution`),
  INDEX `fk_service_contribution_2_idx` (`service_contribution_org_ref` ASC) VISIBLE,
  INDEX `fk_service_contribution_3_idx` (`service_contribution_user_ref` ASC) VISIBLE,
  INDEX `fk_service_contribution_4_idx` (`service_contribution_person_ref` ASC) VISIBLE,
  INDEX `fk_service_contribution_5_idx` (`service_ref` ASC) VISIBLE,
  INDEX `fk_service_contribution_6_idx` (`service_contribution_provider_ref` ASC) VISIBLE,
  CONSTRAINT `fk_service_contribution_2`
    FOREIGN KEY (`service_contribution_org_ref`)
    REFERENCES `gluttex`.`provider_organisation` (`idprovider_organisation`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_service_contribution_3`
    FOREIGN KEY (`service_contribution_user_ref`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_service_contribution_4`
    FOREIGN KEY (`service_contribution_person_ref`)
    REFERENCES `gluttex`.`person` (`id_person`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_service_contribution_5`
    FOREIGN KEY (`service_ref`)
    REFERENCES `gluttex`.`provided_service` (`provided_service_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_service_contribution_6`
    FOREIGN KEY (`service_contribution_provider_ref`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`delivery_broker`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`delivery_broker` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`delivery_broker` (
  `id_delivery_broker` INT NOT NULL,
  `delivery_broker_name` VARCHAR(255) NULL,
  `delivery_broker_label` VARCHAR(255) NULL,
  `delivery_broker_logo_url` VARCHAR(255) NULL,
  `delivery_broker_image_url` VARCHAR(255) NULL,
  `delivery_broker_wallet_id` INT NULL,
  `delivery_broker_price_matrix` TEXT NULL,
  `verified_delivery_broker` TINYINT NULL,
  PRIMARY KEY (`id_delivery_broker`),
  INDEX `fk_delivery_broker_1_idx` (`delivery_broker_wallet_id` ASC) VISIBLE,
  CONSTRAINT `fk_delivery_broker_1`
    FOREIGN KEY (`delivery_broker_wallet_id`)
    REFERENCES `gluttex`.`wallet` (`id_wallet`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`delivery`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`delivery` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`delivery` (
  `id_delivery` INT NOT NULL AUTO_INCREMENT,
  `recipient_person` INT NULL,
  `recipient_provider` INT NULL,
  `delivery_package_count` VARCHAR(45) NULL,
  `delivery_total_weight` DECIMAL NULL,
  `delivery_cargo_dimensions` VARCHAR(255) NULL,
  `delivery_goods_description` TEXT NULL,
  `hs_code` VARCHAR(255) NULL,
  `delivery_merchant_name` VARCHAR(255) NULL,
  `delivery_shipping_method` ENUM('standard', 'express', 'overnight', 'pickup', 'courier', 'same_day', 'international') NULL DEFAULT 'standard',
  `delivery_special_instructions` VARCHAR(45) NULL,
  `delivery_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `delivery_updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `delivery_status` ENUM('pending', 'processing', 'confirmed', 'shipped', 'in_transit', 'out_for_delivery', 'delivered', 'failed', 'cancelled', 'returned', 'refunded') NULL DEFAULT 'PENDING',
  `delivery_address_id` INT NULL DEFAULT NULL,
  `delivery_current_address_id` INT NULL DEFAULT NULL,
  `delivery_fee` DOUBLE NULL DEFAULT 0.0,
  `delivery_provider_id` INT NULL DEFAULT NULL,
  `delivery_broker_id` INT NULL,
  `delivery_invoice_ref` INT NULL,
  `delivery_source_type` ENUM('cart', 'placed_order') NULL,
  `delivery_source_id` INT NULL,
  PRIMARY KEY (`id_delivery`),
  INDEX `fk_delivery_1_idx` (`delivery_address_id` ASC) VISIBLE,
  INDEX `fk_delivery_2_idx` (`delivery_current_address_id` ASC) VISIBLE,
  INDEX `fk_delivery_4_idx` (`delivery_provider_id` ASC) VISIBLE,
  INDEX `fk_delivery_5_idx` (`delivery_broker_id` ASC) VISIBLE,
  INDEX `fk_delivery_7_idx` (`delivery_invoice_ref` ASC) VISIBLE,
  CONSTRAINT `fk_delivery_1`
    FOREIGN KEY (`delivery_address_id`)
    REFERENCES `gluttex`.`address` (`id_address`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_delivery_2`
    FOREIGN KEY (`delivery_current_address_id`)
    REFERENCES `gluttex`.`address` (`id_address`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_delivery_4`
    FOREIGN KEY (`delivery_provider_id`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_delivery_5`
    FOREIGN KEY (`delivery_broker_id`)
    REFERENCES `gluttex`.`delivery_broker` (`id_delivery_broker`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_delivery_7`
    FOREIGN KEY (`delivery_invoice_ref`)
    REFERENCES `gluttex`.`invoice` (`invoice_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`conversation`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`conversation` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`conversation` (
  `id_conversation` INT NOT NULL AUTO_INCREMENT,
  `conversation_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `conversation_org_id` INT NULL DEFAULT NULL,
  `conversation_provider_id` INT NULL DEFAULT NULL,
  `conversation_sender_user_id` INT NULL,
  `conversation_destination_user_id` INT NULL,
  `conversation_type` ENUM('direct', 'group', 'organization', 'provider') NULL DEFAULT 'direct',
  PRIMARY KEY (`id_conversation`),
  INDEX `fk_message_1_idx` (`conversation_sender_user_id` ASC) VISIBLE,
  INDEX `fk_message_2_idx` (`conversation_destination_user_id` ASC) VISIBLE,
  INDEX `fk_message_3_idx` (`conversation_provider_id` ASC) VISIBLE,
  INDEX `fk_message_4_idx` (`conversation_org_id` ASC) VISIBLE,
  CONSTRAINT `fk_message_1`
    FOREIGN KEY (`conversation_sender_user_id`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_message_2`
    FOREIGN KEY (`conversation_destination_user_id`)
    REFERENCES `gluttex`.`app_user` (`id_app_user`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_message_3`
    FOREIGN KEY (`conversation_provider_id`)
    REFERENCES `gluttex`.`product_provider` (`id_product_provider`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_message_4`
    FOREIGN KEY (`conversation_org_id`)
    REFERENCES `gluttex`.`provider_organisation` (`idprovider_organisation`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`money_transaction`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`money_transaction` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`money_transaction` (
  `id_money_transaction` INT NOT NULL AUTO_INCREMENT,
  `money_transaction_document_url` VARCHAR(255) NULL,
  `money_transaction_amount` DECIMAL(20,8) NULL DEFAULT 0.0,
  `money_transaction_reference` VARCHAR(255) NULL,
  `money_transaction_creation` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `money_transaction_last_updated` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `money_transaction_wallet_source_id` INT NULL,
  `money_transaction_wallet_destination_id` INT NULL,
  `money_transaction_status` ENUM('pending', 'processing', 'completed', 'failed', 'cancelled', 'refunded', 'reversed') NULL DEFAULT 'pending',
  `money_transaction_for_payment` INT NULL,
  PRIMARY KEY (`id_money_transaction`),
  INDEX `fk_money_transaction_1_idx` (`money_transaction_wallet_source_id` ASC) VISIBLE,
  INDEX `fk_money_transaction_2_idx` (`money_transaction_wallet_destination_id` ASC) VISIBLE,
  INDEX `fk_money_transaction_3_idx` (`money_transaction_for_payment` ASC) VISIBLE,
  CONSTRAINT `fk_money_transaction_1`
    FOREIGN KEY (`money_transaction_wallet_source_id`)
    REFERENCES `gluttex`.`wallet` (`id_wallet`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_money_transaction_2`
    FOREIGN KEY (`money_transaction_wallet_destination_id`)
    REFERENCES `gluttex`.`wallet` (`id_wallet`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_money_transaction_3`
    FOREIGN KEY (`money_transaction_for_payment`)
    REFERENCES `gluttex`.`payment` (`payment_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`message`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`message` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`message` (
  `id_message` INT NOT NULL AUTO_INCREMENT,
  `message_content` VARCHAR(255) NULL,
  `message_attached_url` VARCHAR(255) NULL,
  `message_type` ENUM('text', 'image', 'file', 'notification', 'system', 'voice', 'video') NULL DEFAULT 'text',
  `message_created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP,
  `message_delivered_at` TIMESTAMP NULL DEFAULT NULL,
  `message_read_at` TIMESTAMP NULL DEFAULT NULL,
  `message_conversation_id` INT NULL,
  `message_is_from_sender` TINYINT NULL,
  PRIMARY KEY (`id_message`),
  INDEX `fk_message_1_idx` (`message_conversation_id` ASC) VISIBLE,
  CONSTRAINT `fk_message_convo`
    FOREIGN KEY (`message_conversation_id`)
    REFERENCES `gluttex`.`conversation` (`id_conversation`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`ledger_entry`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`ledger_entry` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`ledger_entry` (
  `id_ledger_entry` INT NOT NULL AUTO_INCREMENT,
  `wallet_id` INT NOT NULL,
  `money_transaction_id` INT NOT NULL,
  `ledger_entry_type` ENUM('CREDIT', 'DEBIT') NULL,
  `ledger_entry_amount` DECIMAL(20,8) NULL,
  PRIMARY KEY (`id_ledger_entry`),
  INDEX `fk_ledger_entry_wallet1_idx` (`wallet_id` ASC) VISIBLE,
  INDEX `fk_ledger_entry_money_transaction1_idx` (`money_transaction_id` ASC) VISIBLE,
  CONSTRAINT `fk_ledger_entry_wallet1`
    FOREIGN KEY (`wallet_id`)
    REFERENCES `gluttex`.`wallet` (`id_wallet`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_ledger_entry_money_transaction1`
    FOREIGN KEY (`money_transaction_id`)
    REFERENCES `gluttex`.`money_transaction` (`id_money_transaction`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`product_consumption`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`product_consumption` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`product_consumption` (
  `id_product_consumption` INT NOT NULL AUTO_INCREMENT,
  `resource_req_ref` INT NULL,
  `consuming_service_id` INT NULL,
  `consumed_product_id` INT NULL,
  `product_reserved_quantity` INT NULL,
  INDEX `fk_product_consumption_service_resource_requirement1_idx` (`resource_req_ref` ASC) VISIBLE,
  INDEX `fk_product_consumption_ordered_service1_idx` (`consuming_service_id` ASC) VISIBLE,
  INDEX `fk_product_consumption_product1_idx` (`consumed_product_id` ASC) VISIBLE,
  PRIMARY KEY (`id_product_consumption`),
  CONSTRAINT `fk_product_consumption_service_resource_requirement1`
    FOREIGN KEY (`resource_req_ref`)
    REFERENCES `gluttex`.`service_resource_requirement` (`service_resource_requirement_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_product_consumption_ordered_service1`
    FOREIGN KEY (`consuming_service_id`)
    REFERENCES `gluttex`.`ordered_service` (`ordered_service_id`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_product_consumption_product1`
    FOREIGN KEY (`consumed_product_id`)
    REFERENCES `gluttex`.`product` (`id_product`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`diagnosis`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`diagnosis` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`diagnosis` (
  `id_diagnosis` INT NOT NULL AUTO_INCREMENT,
  `patient_id` INT NULL,
  `doctor_id` INT NULL,
  `diagnosis_date` DATE NULL DEFAULT NULL,
  PRIMARY KEY (`id_diagnosis`),
  INDEX `fk_diagnosis_patient1_idx` (`patient_id` ASC) VISIBLE,
  INDEX `fk_diagnosis_doctor1_idx` (`doctor_id` ASC) VISIBLE,
  CONSTRAINT `fk_diagnosis_patient1`
    FOREIGN KEY (`patient_id`)
    REFERENCES `gluttex`.`patient` (`id_patient`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_diagnosis_doctor1`
    FOREIGN KEY (`doctor_id`)
    REFERENCES `gluttex`.`care_giver` (`id_care_giver`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`prescription`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`prescription` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`prescription` (
  `id_prescription` INT NOT NULL AUTO_INCREMENT,
  `diagnosis_id` INT NULL,
  PRIMARY KEY (`id_prescription`),
  INDEX `fk_prescription_diagnosis1_idx` (`diagnosis_id` ASC) VISIBLE,
  CONSTRAINT `fk_prescription_diagnosis1`
    FOREIGN KEY (`diagnosis_id`)
    REFERENCES `gluttex`.`diagnosis` (`id_diagnosis`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `gluttex`.`prescribed_item`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`prescribed_item` ;

CREATE TABLE IF NOT EXISTS `gluttex`.`prescribed_item` (
  `prescription_id` INT NOT NULL,
  `iproduct_id` INT NOT NULL,
  `prescribed_amount` DOUBLE NULL,
  `prescribed_recurrence` VARCHAR(45) NULL,
  INDEX `fk_table1_prescription1_idx` (`prescription_id` ASC) VISIBLE,
  INDEX `fk_table1_iproduct1_idx` (`iproduct_id` ASC) VISIBLE,
  PRIMARY KEY (`prescription_id`, `iproduct_id`),
  CONSTRAINT `fk_table1_prescription1`
    FOREIGN KEY (`prescription_id`)
    REFERENCES `gluttex`.`prescription` (`id_prescription`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_table1_iproduct1`
    FOREIGN KEY (`iproduct_id`)
    REFERENCES `gluttex`.`iproduct` (`id_iproduct`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;

USE `gluttex` ;
USE `gluttex` ;

-- -----------------------------------------------------
-- Placeholder table for view `gluttex`.`financial_documents_status`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `gluttex`.`financial_documents_status` (`id` INT);

-- -----------------------------------------------------
-- procedure create_wallet
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`create_wallet`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE create_wallet(
    IN p_wallet_type ENUM('user','provider','organization','system','virtual','business'),
    IN p_wallet_currency ENUM('DZD','USD','EUR'),
    IN p_wallet_owner_id INT
)
BEGIN
    DECLARE new_wallet_id INT;
    
    INSERT INTO wallet (
        wallet_type,
        wallet_currency,
        wallet_balance,
        wallet_status
    ) VALUES (
        p_wallet_type,
        p_wallet_currency,
        0.00,
        'pending_verification'
    );
    
    SET new_wallet_id = LAST_INSERT_ID();
    
    -- If owner_id is provided, create association
    IF p_wallet_owner_id IS NOT NULL AND p_wallet_owner_id > 0 THEN
        -- Store owner reference in a separate table or update the owner's table
        -- This depends on your schema structure
        CASE p_wallet_type
            WHEN 'user' THEN
                UPDATE app_user SET app_user_wallet_id = new_wallet_id 
                WHERE id_app_user = p_wallet_owner_id;
            WHEN 'provider' THEN
                UPDATE product_provider SET product_provider_wallet_id = new_wallet_id 
                WHERE id_product_provider = p_wallet_owner_id;
            WHEN 'organization' THEN
                UPDATE provider_organisation SET provider_organisation_wallet_id = new_wallet_id 
                WHERE idprovider_organisation = p_wallet_owner_id;
        END CASE;
    END IF;
    
    SELECT new_wallet_id AS wallet_id;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- function get_wallet_balance
-- -----------------------------------------------------

USE `gluttex`;
DROP function IF EXISTS `gluttex`.`get_wallet_balance`;

DELIMITER $$
USE `gluttex`$$
CREATE FUNCTION get_wallet_balance(p_wallet_id INT)
RETURNS DOUBLE
DETERMINISTIC
READS SQL DATA
BEGIN
    DECLARE balance DOUBLE;
    
    SELECT wallet_balance INTO balance
    FROM wallet
    WHERE id_wallet = p_wallet_id;
    
    RETURN COALESCE(balance, 0.00);
END$$

DELIMITER ;

-- -----------------------------------------------------
-- function is_wallet_valid
-- -----------------------------------------------------

USE `gluttex`;
DROP function IF EXISTS `gluttex`.`is_wallet_valid`;

DELIMITER $$
USE `gluttex`$$
CREATE FUNCTION is_wallet_valid(p_wallet_id INT)
RETURNS BOOLEAN
DETERMINISTIC
READS SQL DATA
BEGIN
    DECLARE valid BOOLEAN;
    DECLARE wallet_status VARCHAR(50);
    
    SELECT wallet_status INTO wallet_status
    FROM wallet
    WHERE id_wallet = p_wallet_id;
    
    SET valid = (wallet_status IN ('active', 'pending_verification'));
    
    RETURN valid;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure process_payment
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`process_payment`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE process_payment(
    IN p_payment_invoice_id INT,
    IN p_payment_amount DECIMAL(15,4),
    IN p_payment_method ENUM('cash','card','bank_transfer','mobile_money','crypto','deposit','wallet','check'),
    IN p_payment_reference VARCHAR(255),
    IN p_payment_notes TEXT,
    IN p_payment_type ENUM('deposit','payment','refund')
)
BEGIN
    DECLARE new_payment_id INT;
    DECLARE invoice_total DECIMAL(15,4);
    DECLARE invoice_paid DECIMAL(15,4);
    DECLARE new_status VARCHAR(50);
    
    -- Start transaction
    START TRANSACTION;
    
    -- Validate invoice exists
    SELECT invoice_total_amount INTO invoice_total
    FROM invoice
    WHERE invoice_id = p_payment_invoice_id
    FOR UPDATE;
    
    IF invoice_total IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Invoice not found';
    END IF;
    
    -- Calculate total paid for this invoice
    SELECT COALESCE(SUM(payment_amount), 0) INTO invoice_paid
    FROM payment
    WHERE payment_invoice_id = p_payment_invoice_id
    AND payment_status != 'cancelled';
    
    -- Determine new invoice status
    IF (invoice_paid + p_payment_amount) >= invoice_total THEN
        SET new_status = 'paid';
    ELSEIF invoice_paid + p_payment_amount > 0 THEN
        SET new_status = 'partially_paid';
    ELSE
        SET new_status = 'unpaid';
    END IF;
    
    -- Create payment record
    INSERT INTO payment (
        payment_invoice_id,
        payment_amount,
        payment_method,
        payment_status,
        payment_reference,
        payment_notes,
        payment_type,
        payment_created_at,
        payment_updated_at
    ) VALUES (
        p_payment_invoice_id,
        p_payment_amount,
        p_payment_method,
        'completed',
        p_payment_reference,
        p_payment_notes,
        p_payment_type,
        NOW(),
        NOW()
    );
    
    SET new_payment_id = LAST_INSERT_ID();
    
    -- Update invoice status
    UPDATE invoice
    SET invoice_status = new_status,
        invoice_updated_at = NOW()
    WHERE invoice_id = p_payment_invoice_id;
    
    COMMIT;
    
    SELECT new_payment_id AS payment_id;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure create_money_transaction
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`create_money_transaction`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE create_money_transaction(
    IN p_source_wallet_id INT,
    IN p_destination_wallet_id INT,
    IN p_amount DOUBLE,
    IN p_reference VARCHAR(255),
    IN p_document_url VARCHAR(255),
    IN p_payment_id INT
)
BEGIN
    DECLARE new_transaction_id INT;
    DECLARE source_balance DOUBLE;
    DECLARE source_status VARCHAR(50);
    DECLARE dest_status VARCHAR(50);
    
    -- Start transaction
    START TRANSACTION;
    
    -- Validate source wallet
    SELECT wallet_balance, wallet_status INTO source_balance, source_status
    FROM wallet
    WHERE id_wallet = p_source_wallet_id
    FOR UPDATE;
    
    IF source_balance IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Source wallet not found';
    END IF;
    
    IF source_status != 'active' THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Source wallet is not active';
    END IF;
    
    IF source_balance < p_amount THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Insufficient balance in source wallet';
    END IF;
    
    -- Validate destination wallet
    SELECT wallet_status INTO dest_status
    FROM wallet
    WHERE id_wallet = p_destination_wallet_id
    FOR UPDATE;
    
    IF dest_status IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Destination wallet not found';
    END IF;
    
    IF dest_status != 'active' THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Destination wallet is not active';
    END IF;
    
    -- Create transaction record
    INSERT INTO money_transaction (
        money_transaction_amount,
        money_transaction_reference,
        money_transaction_document_url,
        money_transaction_wallet_source_id,
        money_transaction_wallet_destination_id,
        money_transaction_status,
        money_transaction_for_payment,
        money_transaction_creation,
        money_transaction_last_updated
    ) VALUES (
        p_amount,
        p_reference,
        p_document_url,
        p_source_wallet_id,
        p_destination_wallet_id,
        'completed',
        p_payment_id,
        NOW(),
        NOW()
    );
    
    SET new_transaction_id = LAST_INSERT_ID();
    
    -- Update source wallet balance
    UPDATE wallet
    SET wallet_balance = wallet_balance - p_amount
    WHERE id_wallet = p_source_wallet_id;
    
    -- Update destination wallet balance
    UPDATE wallet
    SET wallet_balance = wallet_balance + p_amount
    WHERE id_wallet = p_destination_wallet_id;
    
    COMMIT;
    
    SELECT new_transaction_id AS transaction_id;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure process_refund
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`process_refund`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE process_refund(
    IN p_original_transaction_id INT,
    IN p_refund_amount DOUBLE,
    IN p_refund_reason VARCHAR(255)
)
BEGIN
    DECLARE original_source INT;
    DECLARE original_destination INT;
    DECLARE original_amount DOUBLE;
    DECLARE original_status VARCHAR(50);
    
    -- Start transaction
    START TRANSACTION;
    
    -- Get original transaction details
    SELECT 
        money_transaction_wallet_source_id,
        money_transaction_wallet_destination_id,
        money_transaction_amount,
        money_transaction_status
    INTO 
        original_source,
        original_destination,
        original_amount,
        original_status
    FROM money_transaction
    WHERE id_money_transaction = p_original_transaction_id
    FOR UPDATE;
    
    IF original_source IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Original transaction not found';
    END IF;
    
    IF original_status = 'refunded' THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Transaction already refunded';
    END IF;
    
    -- Validate refund amount
    IF p_refund_amount > original_amount THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Refund amount exceeds original transaction amount';
    END IF;
    
    -- Create refund transaction (reverse direction)
    INSERT INTO money_transaction (
        money_transaction_amount,
        money_transaction_reference,
        money_transaction_wallet_source_id,
        money_transaction_wallet_destination_id,
        money_transaction_status,
        money_transaction_document_url,
        money_transaction_creation,
        money_transaction_last_updated
    ) VALUES (
        p_refund_amount,
        CONCAT('REFUND_', p_original_transaction_id),
        original_destination,
        original_source,
        'completed',
        NULL,
        NOW(),
        NOW()
    );
    
    -- Update source wallet balance (original destination)
    UPDATE wallet
    SET wallet_balance = wallet_balance - p_refund_amount
    WHERE id_wallet = original_destination;
    
    -- Update destination wallet balance (original source)
    UPDATE wallet
    SET wallet_balance = wallet_balance + p_refund_amount
    WHERE id_wallet = original_source;
    
    -- Mark original transaction as refunded
    UPDATE money_transaction
    SET money_transaction_status = 'refunded',
        money_transaction_last_updated = NOW()
    WHERE id_money_transaction = p_original_transaction_id;
    
    COMMIT;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure transfer_between_wallets
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`transfer_between_wallets`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE transfer_between_wallets(
    IN p_from_wallet INT,
    IN p_to_wallet INT,
    IN p_amount DOUBLE,
    IN p_reference VARCHAR(255)
)
BEGIN
    CALL create_money_transaction(
        p_from_wallet,
        p_to_wallet,
        p_amount,
        p_reference,
        NULL,
        NULL
    );
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure deposit_to_wallet
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`deposit_to_wallet`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE deposit_to_wallet(
    IN p_wallet_id INT,
    IN p_amount DOUBLE,
    IN p_reference VARCHAR(255),
    IN p_payment_id INT
)
BEGIN
    -- System wallet as source (virtual system account)
    -- You'll need a system wallet ID, e.g., 1
    DECLARE system_wallet_id INT DEFAULT 1;
    
    CALL create_money_transaction(
        system_wallet_id,
        p_wallet_id,
        p_amount,
        p_reference,
        NULL,
        p_payment_id
    );
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure withdraw_from_wallet
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`withdraw_from_wallet`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE withdraw_from_wallet(
    IN p_wallet_id INT,
    IN p_amount DOUBLE,
    IN p_reference VARCHAR(255),
    IN p_payment_id INT
)
BEGIN
    -- System wallet as destination
    DECLARE system_wallet_id INT DEFAULT 1;
    
    CALL create_money_transaction(
        p_wallet_id,
        system_wallet_id,
        p_amount,
        p_reference,
        NULL,
        p_payment_id
    );
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure get_wallet_transactions
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`get_wallet_transactions`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE get_wallet_transactions(
    IN p_wallet_id INT,
    IN p_limit INT,
    IN p_offset INT
)
BEGIN
    SELECT 
        mt.id_money_transaction,
        mt.money_transaction_amount,
        mt.money_transaction_reference,
        mt.money_transaction_status,
        mt.money_transaction_creation,
        mt.money_transaction_document_url,
        mt.money_transaction_for_payment,
        -- Source wallet info
        source_wallet.id_wallet AS source_wallet_id,
        source_wallet.wallet_type AS source_wallet_type,
        -- Destination wallet info
        dest_wallet.id_wallet AS dest_wallet_id,
        dest_wallet.wallet_type AS dest_wallet_type,
        -- Payment info if exists
        p.payment_id,
        p.payment_amount,
        p.payment_method,
        p.payment_status
    FROM money_transaction mt
    LEFT JOIN wallet source_wallet ON mt.money_transaction_wallet_source_id = source_wallet.id_wallet
    LEFT JOIN wallet dest_wallet ON mt.money_transaction_wallet_destination_id = dest_wallet.id_wallet
    LEFT JOIN payment p ON mt.money_transaction_for_payment = p.payment_id
    WHERE mt.money_transaction_wallet_source_id = p_wallet_id
       OR mt.money_transaction_wallet_destination_id = p_wallet_id
    ORDER BY mt.money_transaction_creation DESC
    LIMIT p_limit OFFSET p_offset;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure get_wallet_summary
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`get_wallet_summary`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE get_wallet_summary(
    IN p_wallet_id INT
)
BEGIN
    DECLARE balance DOUBLE;
    DECLARE total_in DOUBLE;
    DECLARE total_out DOUBLE;
    DECLARE pending_in DOUBLE;
    DECLARE pending_out DOUBLE;
    
    -- Get current balance
    SELECT wallet_balance INTO balance
    FROM wallet
    WHERE id_wallet = p_wallet_id;
    
    -- Calculate totals
    SELECT 
        COALESCE(SUM(CASE 
            WHEN money_transaction_wallet_destination_id = p_wallet_id 
            AND money_transaction_status = 'completed' 
            THEN money_transaction_amount 
            ELSE 0 
        END), 0) INTO total_in
    FROM money_transaction;
    
    SELECT 
        COALESCE(SUM(CASE 
            WHEN money_transaction_wallet_source_id = p_wallet_id 
            AND money_transaction_status = 'completed' 
            THEN money_transaction_amount 
            ELSE 0 
        END), 0) INTO total_out
    FROM money_transaction;
    
    SELECT 
        COALESCE(SUM(CASE 
            WHEN money_transaction_wallet_destination_id = p_wallet_id 
            AND money_transaction_status = 'pending' 
            THEN money_transaction_amount 
            ELSE 0 
        END), 0) INTO pending_in
    FROM money_transaction;
    
    SELECT 
        COALESCE(SUM(CASE 
            WHEN money_transaction_wallet_source_id = p_wallet_id 
            AND money_transaction_status = 'pending' 
            THEN money_transaction_amount 
            ELSE 0 
        END), 0) INTO pending_out
    FROM money_transaction;
    
    -- Return summary
    SELECT 
        balance AS current_balance,
        total_in AS total_credited,
        total_out AS total_debited,
        (total_in - total_out) AS net_balance,
        pending_in AS pending_credits,
        pending_out AS pending_debits,
        (pending_in - pending_out) AS pending_net;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure reverse_transaction
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`reverse_transaction`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE reverse_transaction(
    IN p_transaction_id INT,
    IN p_reason VARCHAR(255)
)
BEGIN
    DECLARE source_id INT;
    DECLARE dest_id INT;
    DECLARE amount DOUBLE;
    DECLARE current_status VARCHAR(50);
    
    -- Start transaction
    START TRANSACTION;
    
    -- Get transaction details
    SELECT 
        money_transaction_wallet_source_id,
        money_transaction_wallet_destination_id,
        money_transaction_amount,
        money_transaction_status
    INTO 
        source_id,
        dest_id,
        amount,
        current_status
    FROM money_transaction
    WHERE id_money_transaction = p_transaction_id
    FOR UPDATE;
    
    IF source_id IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Transaction not found';
    END IF;
    
    IF current_status != 'completed' THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Only completed transactions can be reversed';
    END IF;
    
    -- Reverse the transaction
    INSERT INTO money_transaction (
        money_transaction_amount,
        money_transaction_reference,
        money_transaction_wallet_source_id,
        money_transaction_wallet_destination_id,
        money_transaction_status,
        money_transaction_document_url,
        money_transaction_creation,
        money_transaction_last_updated
    ) VALUES (
        amount,
        CONCAT('REVERSAL_', p_transaction_id),
        dest_id,
        source_id,
        'completed',
        NULL,
        NOW(),
        NOW()
    );
    
    -- Update wallet balances
    UPDATE wallet SET wallet_balance = wallet_balance - amount 
    WHERE id_wallet = dest_id;
    
    UPDATE wallet SET wallet_balance = wallet_balance + amount 
    WHERE id_wallet = source_id;
    
    -- Mark original as reversed
    UPDATE money_transaction
    SET money_transaction_status = 'reversed',
        money_transaction_last_updated = NOW()
    WHERE id_money_transaction = p_transaction_id;
    
    COMMIT;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- function get_invoice_payment_status
-- -----------------------------------------------------

USE `gluttex`;
DROP function IF EXISTS `gluttex`.`get_invoice_payment_status`;

DELIMITER $$
USE `gluttex`$$
CREATE FUNCTION get_invoice_payment_status(p_invoice_id INT)
RETURNS VARCHAR(50)
DETERMINISTIC
READS SQL DATA
BEGIN
    DECLARE total DECIMAL(15,4);
    DECLARE paid DECIMAL(15,4);
    DECLARE status VARCHAR(50);
    
    SELECT invoice_total_amount INTO total
    FROM invoice
    WHERE invoice_id = p_invoice_id;
    
    SELECT COALESCE(SUM(payment_amount), 0) INTO paid
    FROM payment
    WHERE payment_invoice_id = p_invoice_id
    AND payment_status != 'cancelled';
    
    IF paid = 0 THEN
        SET status = 'unpaid';
    ELSEIF paid >= total THEN
        SET status = 'paid';
    ELSE
        SET status = 'partially_paid';
    END IF;
    
    RETURN status;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure process_cart_payment
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`process_cart_payment`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE process_cart_payment(
    IN p_cart_id INT,
    IN p_payment_method ENUM('cash','card','bank_transfer','mobile_money','crypto','deposit','wallet','check'),
    IN p_amount DECIMAL(15,4),
    IN p_payment_reference VARCHAR(255)
)
BEGIN
    DECLARE invoice_id INT;
    DECLARE payment_id INT;
    
    -- Start transaction
    START TRANSACTION;
    
    -- Get or create invoice for cart
    SELECT cart_invoice INTO invoice_id
    FROM cart
    WHERE cart_id = p_cart_id;
    
    IF invoice_id IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Cart has no invoice';
    END IF;
    
    -- Process payment
    CALL process_payment(
        invoice_id,
        p_amount,
        p_payment_method,
        p_payment_reference,
        CONCAT('Payment for cart #', p_cart_id),
        'payment'
    );
    
    -- Get the payment ID
    SET payment_id = LAST_INSERT_ID();
    
    -- Update cart status
    UPDATE cart
    SET cart_status = 'completed',
        cart_updated_at = NOW()
    WHERE cart_id = p_cart_id;
    
    COMMIT;
    
    SELECT payment_id AS payment_id;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure create_ordered_service
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`create_ordered_service`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE  create_ordered_service(
    IN p_cart_id INT,
    IN p_service_id INT,
    IN p_quantity INT,
    IN p_unit_price DECIMAL(15,4),
    IN p_total_price DECIMAL(15,4),
    IN p_notes TEXT,
    IN p_scheduled_at TIMESTAMP,
    IN p_delivery_fee DOUBLE,
    IN p_delivery_status VARCHAR(50),
    OUT p_ordered_service_id INT,
    OUT p_success BOOLEAN,
    OUT p_error_message VARCHAR(255)
)
BEGIN
    DECLARE v_service_exists INT DEFAULT 0;
    DECLARE v_is_active TINYINT DEFAULT 0;
    DECLARE v_cart_exists INT DEFAULT 0;
    DECLARE v_cart_status VARCHAR(50) DEFAULT '';
    
    -- Declare exit handler for errors
    DECLARE EXIT HANDLER FOR SQLEXCEPTION
    BEGIN
        GET DIAGNOSTICS CONDITION 1
            p_error_message = MESSAGE_TEXT;
        SET p_success = FALSE;
        ROLLBACK;
        SIGNAL SQLSTATE '45000'
SET MESSAGE_TEXT = '...';
    END;
    
    -- Start transaction
    START TRANSACTION;
    
    -- Initialize output
    SET p_success = FALSE;
    SET p_error_message = '';
    SET p_ordered_service_id = 0;
    
    -- Validate cart exists and is in valid status
    SELECT COUNT(*) INTO v_cart_exists
    FROM cart
    WHERE cart_id = p_cart_id;
    
    IF v_cart_exists = 0 THEN
        SET p_error_message = 'Cart not found';
        ROLLBACK;
        SIGNAL SQLSTATE '45000'
		SET MESSAGE_TEXT = 'Cart not found';
    END IF;
    
    -- Get cart status
    SELECT cart_status INTO v_cart_status
    FROM cart
    WHERE cart_id = p_cart_id;
    
    -- Check if cart status is valid (using the actual status values in your DB)
    IF v_cart_status NOT IN ('PENDING', 'OPEN', 'CHECKOUT', 'pending', 'open', 'checkout') THEN
        SET p_error_message = CONCAT('Cart is not in a valid status for adding services. Current status: ', v_cart_status);
        SIGNAL SQLSTATE '45000'
SET MESSAGE_TEXT = 'Cart not found';
        ROLLBACK;
    END IF;
    
    -- Validate service exists and is active
SELECT provided_service_is_active
INTO v_is_active
FROM provided_service
WHERE provided_service_id = p_service_id
LIMIT 1;
    
    IF v_service_exists = 0 THEN
        SET p_error_message = 'Service not found';
        ROLLBACK;
        SIGNAL SQLSTATE '45000'
SET MESSAGE_TEXT = 'Cart not found';
    END IF;
    
    IF v_is_active = 0 OR v_is_active IS NULL THEN
        SET p_error_message = 'Service is not active';
        ROLLBACK;
        SIGNAL SQLSTATE '45000'
SET MESSAGE_TEXT = 'Cart not found';
    END IF;
    
    
    
    -- Validate total price is valid
    IF p_total_price < 0 THEN
        SET p_error_message = 'Total price cannot be negative';
        ROLLBACK;
        SIGNAL SQLSTATE '45000'
SET MESSAGE_TEXT = 'Cart not found';
    END IF;
    
    IF p_quantity <= 0 THEN
        SET p_error_message = 'Quantity must be greater than 0';
        ROLLBACK;
        SIGNAL SQLSTATE '45000'
SET MESSAGE_TEXT = 'Cart not found';
    END IF;
    
    -- Create ordered service
    INSERT INTO ordered_service (
        ordered_service_cart_id,
        ordered_service_service_id,
        ordered_service_quantity,
        ordered_service_unit_price,
        ordered_service_total_price,
        ordered_service_notes,
        ordered_service_scheduled_at,
        ordered_service_delivery_fee,
        ordered_service_delivery_status
    ) VALUES (
        p_cart_id,
        p_service_id,
        p_quantity,
        p_unit_price,
        p_total_price,
        p_notes,
        p_scheduled_at,
        COALESCE(p_delivery_fee, 0),
        COALESCE(p_delivery_status, 'PENDING')
    );
    
    SET p_ordered_service_id = LAST_INSERT_ID();
    
    -- Update cart total amount
    UPDATE cart
    SET cart_total_amount = COALESCE(cart_total_amount, 0) + p_total_price,
        cart_updated_at = NOW()
    WHERE cart_id = p_cart_id;
    
    SET p_success = TRUE;
    
    COMMIT;
    
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure cancel_ordered_service
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`cancel_ordered_service`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE cancel_ordered_service(
    IN p_ordered_service_id INT,
    IN p_cancellation_reason VARCHAR(255),
    OUT p_success BOOLEAN,
    OUT p_error_message VARCHAR(255)
)
BEGIN

    DECLARE v_current_status VARCHAR(50);
    DECLARE v_cart_id INT;
    DECLARE v_service_price DECIMAL(15,4);

    DECLARE EXIT HANDLER FOR SQLEXCEPTION
    BEGIN
        SET p_success = FALSE;
        SET p_error_message = 'Transaction failed';
        ROLLBACK;
    END;

    START TRANSACTION;

    SET p_success = FALSE;
    SET p_error_message = '';

    SELECT
        ordered_service_delivery_status,
        ordered_service_cart_id,
        ordered_service_total_price
    INTO
        v_current_status,
        v_cart_id,
        v_service_price
    FROM ordered_service
    WHERE ordered_service_id = p_ordered_service_id
    FOR UPDATE;

    IF v_current_status IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Ordered service not found';
    END IF;

    IF v_current_status = 'cancelled' THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Already cancelled';
    END IF;

    IF v_current_status NOT IN ('pending','processing','scheduled') THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Invalid status for cancellation';
    END IF;

    UPDATE ordered_service
    SET ordered_service_delivery_status = 'cancelled',
        ordered_service_notes = CONCAT(
            COALESCE(ordered_service_notes,''),
            ' | Cancelled: ',
            COALESCE(p_cancellation_reason,'No reason provided')
        ),
        ordered_service_updated_at = NOW()
    WHERE ordered_service_id = p_ordered_service_id;

    UPDATE cart
    SET cart_total_amount =
        GREATEST(cart_total_amount - v_service_price, 0),
        cart_updated_at = NOW()
    WHERE cart_id = v_cart_id;

    CALL restore_service_resources(p_ordered_service_id);

    COMMIT;

    SET p_success = TRUE;

END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure restore_service_resources
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`restore_service_resources`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE restore_service_resources(
    IN p_ordered_service_id INT
)
BEGIN
    DECLARE v_service_id INT;
    DECLARE v_quantity INT;
    DECLARE done INT DEFAULT FALSE;
    DECLARE v_product_id INT;
    DECLARE v_quantity_to_restore INT;
    
    DECLARE cur_resources CURSOR FOR
        SELECT 
            srr.service_resource_requirement_product_ref,
            srr.service_resource_requirement_quantity * os.ordered_service_quantity
        FROM service_resource_requirement srr
        JOIN ordered_service os ON srr.service_resource_requirement_service_id = os.ordered_service_service_id
        WHERE os.ordered_service_id = p_ordered_service_id
        AND srr.service_resource_requirement_is_consumable = 1
        AND srr.service_resource_requirement_product_ref IS NOT NULL;
    
    DECLARE CONTINUE HANDLER FOR NOT FOUND SET done = TRUE;
    
    -- Get the service and quantity for this ordered service
    SELECT 
        ordered_service_service_id,
        ordered_service_quantity
    INTO 
        v_service_id,
        v_quantity
    FROM ordered_service
    WHERE ordered_service_id = p_ordered_service_id;
    
    -- Restore stock for each consumable resource
    OPEN cur_resources;
    
    read_loop: LOOP
        FETCH cur_resources INTO v_product_id, v_quantity_to_restore;
        IF done THEN
            LEAVE read_loop;
        END IF;
        
        -- Restore product stock
        UPDATE product
        SET product_quantity = product_quantity + v_quantity_to_restore
        WHERE id_product = v_product_id;
        
    END LOOP;
    
    CLOSE cur_resources;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure create_ordered_item
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`create_ordered_item`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE create_ordered_item(
    IN p_cart_id INT,
    IN p_product_id INT,
    IN p_quantity INT,
    IN p_applied_vat DOUBLE,
    IN p_unit_price DOUBLE,
    IN p_product_discount DOUBLE,
    IN p_delivery_fee DOUBLE,
    IN p_delivery_status VARCHAR(50),
    OUT p_ordered_item_id INT,
    OUT p_success BOOLEAN,
    OUT p_error_message VARCHAR(255)
)
BEGIN

    DECLARE v_product_quantity INT;
    DECLARE v_product_price DOUBLE;
    DECLARE v_cart_status VARCHAR(50);
    DECLARE v_total_price DOUBLE;

    DECLARE EXIT HANDLER FOR SQLEXCEPTION
    BEGIN
        GET DIAGNOSTICS CONDITION 1
            p_error_message = MESSAGE_TEXT;

        SET p_success = FALSE;

        ROLLBACK;
    END;

    START TRANSACTION;

    SET p_success = FALSE;
    SET p_error_message = '';
    SET p_ordered_item_id = NULL;

    SELECT cart_status
    INTO v_cart_status
    FROM cart
    WHERE cart_id = p_cart_id
    FOR UPDATE;

    IF v_cart_status IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Cart not found';
    END IF;

    IF LOWER(v_cart_status) NOT IN ('open','pending','checkout') THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Cart is not in a valid status for adding items';
    END IF;

    SELECT
        product_quantity,
        product_price
    INTO
        v_product_quantity,
        v_product_price
    FROM product
    WHERE id_product = p_product_id
    FOR UPDATE;

    IF v_product_quantity IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Product not found';
    END IF;

    IF p_quantity <= 0 THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Quantity must be greater than zero';
    END IF;

    IF v_product_quantity < p_quantity THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Insufficient stock';
    END IF;

    SET v_total_price =
        (p_unit_price * p_quantity)
        * (1 + p_applied_vat / 100)
        - (p_product_discount * p_quantity);

    INSERT INTO ordered_item (
        ordered_product_id,
        ordered_quantity,
        applied_vat,
        unit_price,
        product_discount,
        ordered_item_cart_ref,
        ordered_item_delivery_status,
        ordered_item_delivery_fee
    )
    VALUES (
        p_product_id,
        p_quantity,
        p_applied_vat,
        p_unit_price,
        p_product_discount,
        p_cart_id,
        COALESCE(p_delivery_status, 'pending'),
        COALESCE(p_delivery_fee, 0)
    );

    SET p_ordered_item_id = LAST_INSERT_ID();

    UPDATE product
    SET product_quantity = product_quantity - p_quantity,
        last_updated = NOW()
    WHERE id_product = p_product_id;

    UPDATE cart
    SET cart_total_amount =
        COALESCE(cart_total_amount, 0) + v_total_price,
        cart_updated_at = NOW()
    WHERE cart_id = p_cart_id;

    COMMIT;

    SET p_success = TRUE;

END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure cancel_ordered_item
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`cancel_ordered_item`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE cancel_ordered_item(
    IN p_ordered_item_id INT,
    IN p_cancellation_reason VARCHAR(255),
    OUT p_success BOOLEAN,
    OUT p_error_message VARCHAR(255)
)
BEGIN

    DECLARE v_product_id INT;
    DECLARE v_quantity INT;
    DECLARE v_unit_price DOUBLE;
    DECLARE v_product_discount DOUBLE;
    DECLARE v_applied_vat DOUBLE;
    DECLARE v_cart_id INT;
    DECLARE v_current_status VARCHAR(50);
    DECLARE v_total_price DOUBLE;

    DECLARE EXIT HANDLER FOR SQLEXCEPTION
    BEGIN
        GET DIAGNOSTICS CONDITION 1
            p_error_message = MESSAGE_TEXT;

        SET p_success = FALSE;
        ROLLBACK;
    END;

    START TRANSACTION;

    SET p_success = FALSE;
    SET p_error_message = '';

    -- Lock ordered item row
    SELECT
        ordered_product_id,
        ordered_quantity,
        unit_price,
        product_discount,
        applied_vat,
        ordered_item_cart_ref,
        ordered_item_delivery_status
    INTO
        v_product_id,
        v_quantity,
        v_unit_price,
        v_product_discount,
        v_applied_vat,
        v_cart_id,
        v_current_status
    FROM ordered_item
    WHERE id_ordered_item = p_ordered_item_id
    FOR UPDATE;

    IF v_product_id IS NULL THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Ordered item not found';
    END IF;

    IF v_current_status = 'cancelled' THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Item already cancelled';
    END IF;

    IF v_current_status NOT IN ('pending', 'processing') THEN
        SIGNAL SQLSTATE '45000'
        SET MESSAGE_TEXT = 'Item cannot be cancelled in current status';
    END IF;

    SET v_total_price =
        (v_unit_price * v_quantity)
        * (1 + v_applied_vat / 100)
        - (v_product_discount * v_quantity);

    UPDATE ordered_item
    SET ordered_item_delivery_status = 'cancelled'
    WHERE id_ordered_item = p_ordered_item_id;

    UPDATE product
    SET product_quantity = product_quantity + v_quantity,
        last_updated = NOW()
    WHERE id_product = v_product_id;

    UPDATE cart
    SET cart_total_amount =
        GREATEST(cart_total_amount - v_total_price, 0),
        cart_updated_at = NOW()
    WHERE cart_id = v_cart_id;

    COMMIT;

    SET p_success = TRUE;

END$$

DELIMITER ;

-- -----------------------------------------------------
-- procedure restore_ordered_item_stock
-- -----------------------------------------------------

USE `gluttex`;
DROP procedure IF EXISTS `gluttex`.`restore_ordered_item_stock`;

DELIMITER $$
USE `gluttex`$$
CREATE PROCEDURE restore_ordered_item_stock(
    IN p_ordered_item_id INT
)
BEGIN
    DECLARE v_product_id INT;
    DECLARE v_quantity INT;
    
    -- Get product and quantity
    SELECT 
        ordered_product_id,
        ordered_quantity
    INTO 
        v_product_id,
        v_quantity
    FROM ordered_item
    WHERE id_ordered_item = p_ordered_item_id;
    
    -- Restore stock
    UPDATE product
    SET product_quantity = product_quantity + v_quantity,
        last_updated = NOW()
    WHERE id_product = v_product_id;
END$$

DELIMITER ;

-- -----------------------------------------------------
-- View `gluttex`.`financial_documents_status`
-- -----------------------------------------------------
DROP TABLE IF EXISTS `gluttex`.`financial_documents_status`;
DROP VIEW IF EXISTS `gluttex`.`financial_documents_status` ;
USE `gluttex`;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
